# Experiment log

**Best so far:** 55% (experiment 8)

| # | Date | Change | Hypothesis | Success % | Mean dist (cm) | Median dist (cm) | Verdict |
|---|------|--------|------------|-----------|----------------|------------------|---------|
| 0 | 2026-08-25 | baseline (VecNormalize + current reward/hyperparams) | reference point for the ratchet | 6 | 12.3 | 12.9 | kept |
| 1 | 2026-08-25 | train.py: PPO n_steps 1024 -> 256 (all else identical) | 30k steps with n_steps=1024 gives only ~29 policy updates — too few to fit policy+value function (baseline median dist 12.9cm ≈ untrained). Rollouts of 256 give ~117 updates in the same step budget; more frequent updates should massively improve sample efficiency. Expect large gain. | 6 | 12.3 | 12.9 | reverted (equal) |

Note (exp 1): training-rollout success_rate peaked ~33% early then decayed to ~18%; ep_rew_mean also declined (8.9 -> 5.7) — more updates alone did not produce a better deterministic policy; behavior looks like noise-driven contact rather than directed reaching. Next candidates: reward structure (e.g. velocity/direction shaping or success-region dwell bonus), exploration (ent_coef), or lr schedule.
| 2 | 2026-08-25 | reach_reward.py: ACTION_COST_COEFFICIENT 0.0 -> 0.05 | Actions are currently free, so saturating both motors (quadratic sum 2.0/step) is a viable noise-farming strategy that the dense progress term intermittently rewards — matching exp 1's "noise-driven contact" diagnosis. A quadratic energy tax of 0.05 makes flailing cost ~0.1/step (comparable to the per-step progress signal, ~20 over an episode) while costing deliberate motion far less, pushing the policy toward economical, directed reaching instead of rewarded noise. Expect higher success rate and lower median distance. | 14 | 10.9 | 10.9 | kept |

Diff summary (exp 2, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (ACTION_COST_COEFFICIENT = 0.05).

Note (exp 2): first gain over baseline (+8 pts). Training rollout success still showed exp 1's peak-then-decay shape (0.35 @ 3k -> 0.23 @ 30k) and ep_rew_mean dipped negative mid-run before recovering, so the energy tax alone does not cure the late-run decay; it mainly made the final policy more directed at eval time (median 12.9 -> 10.9 cm). The decay signature suggests optimization instability remains the binding constraint — next candidates: lower constant LR (schedule callables would break run_config.json serialization), SUCCESS_BONUS reduction (25 vs ~2.8 total achievable dense progress may destabilize value fitting), or gamma reduction for tighter credit assignment.
| 3 | 2026-08-25 | reach_reward.py: SUCCESS_BONUS 25.0 -> 5.0 | Structural fix for the twice-diagnosed late-run instability: the sparse bonus is ~9x the max achievable dense progress (~2.8), so value targets jump by +/-25 exactly at the 3 cm boundary while success is still rare (14%); these rare huge bonuses dominate returns and keep value fitting noisy, producing the observed peak-then-decay. Shrinking the bonus to ~1.8x dense progress keeps an explicit enter-and-dwell incentive while cutting the value-target discontinuity 5x; expect stabler late training and higher final success. | 40 | 8.6 | 7.3 | kept |

Diff summary (exp 3, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (SUCCESS_BONUS = 5.0).

Note (exp 3): hypothesis confirmed on both counts. Training rollout success no longer decayed (held ~0.30-0.35 from 14k steps to the end instead of dropping to ~0.2), ep_rew_mean improved monotonically (-6.3 -> -4.2), and eval success jumped 14% -> 40% (median dist 10.9 -> 7.3 cm). Rebalancing sparse vs dense reward scales was the binding-constraint fix; remaining gap is precision in the final ~4-7 cm band — next candidates: dwell/per-step bonus inside threshold (reward staying, not just arriving), PROGRESS_COEFFICIENT increase near target, or lower constant LR to consolidate the now-stable learning curve further.

Diff summary (exp 3, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (SUCCESS_BONUS = 5.0).

| 4 | 2026-08-25 | reach_reward.py: near-zone progress boost — when `current_distance <= NEAR_ZONE_RADIUS` (0.10 m), add `NEAR_ZONE_PROGRESS_BOOST * progress` with boost 20.0 on top of the base coefficient (effective 30/m inside vs 10/m outside) | All remaining failures live in the final precision band (median 7.3 cm vs threshold 3 cm; env terminates on entry so there is no dwell issue). In that band the dense signal is only 0.01 per mm — negligible against rollout noise — so the value function barely discriminates near-target states and the policy plateaus just outside the ball. Amplifying the progress gradient 3x within 0.10 m sharpens near-target value estimates and makes closing the last centimeters worth ~1.5 extra return over a typical 5 cm approach, without touching far-field behavior or re-inflating the sparse bonus that exp 3 had to shrink. Expect higher success rate and lower median distance. | 7 | 12.3 | 13.0 | reverted (worse) |
| 5 | 2026-08-25 | reach_reward.py: add smooth exponentially-saturating closeness shaping — new constants CLOSENESS_COEFFICIENT = 2.0 and CLOSENESS_LENGTH_SCALE = 0.05 m, reward gains term `C*(exp(-current_d/λ) - exp(-previous_d/λ))` | Applies exp 4 lesson (b): scale the near-target gradient WITHOUT any distance discontinuity. Linear progress pays a flat 10/m everywhere, so in the failing final band (~7 cm median vs 3 cm threshold) the value function gets almost no extra signal for being close. An exponential closeness potential Phi(d) = C*exp(-d/λ), applied potential-based (Phi(d_now) - Phi(d_prev)), raises the effective gradient continuously from ~15/m at 8 cm to ~32/m at 3 cm while staying negligible beyond ~15 cm — sharpening value estimates exactly where failures concentrate, with zero kinks at any boundary (unlike exp 4's failed hard gate). Magnitude is modest: max ~0.7 extra return for a full 8->3 cm approach vs 5.0 bonus and ~0.5 linear progress, so sparse/dense balance from exp 3 is preserved. Expect higher success rate and lower median distance. | 44 | 7.5 | 3.9 | kept |

Diff summary (exp 5, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 9 +++++++` (added CLOSENESS_COEFFICIENT = 2.0, CLOSENESS_LENGTH_SCALE = 0.05, _closeness_potential helper, potential-based term in reach_reward).

Note (exp 5): hypothesis confirmed — new best, 40% -> 44% overall and median distance 7.3 -> 3.9 cm (mean 8.6 -> 7.5). Training dynamics were the healthiest of all runs: after an early dip (~0.16-0.18 @ 4-6k), rollout success climbed monotonically to 0.56 by 30k (vs 0.30-0.35 plateaus in exps 2-4), ep_rew_mean rose -7.5 -> -0.59, explained_variance reached 0.87-0.93, and there was no late-run decay — the continuous gradient scaling fixed the value-fitting noise without any boundary kink. The median now sits just above the 3 cm threshold while the mean is dragged by a long tail (worst 23.4 cm), so remaining failures split into near-misses (~4-6 cm) and occasional far misses. Next candidates: raise CLOSENESS_COEFFICIENT (e.g. 4.0, same structure — gradient boost doubled but still smooth and small vs bonus), shorten CLOSENESS_LENGTH_SCALE for even sharper final-band focus, or lower constant LR to polish precision given the now-stable curve.

Diff summary (exp 4, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 7 +++++-` (added NEAR_ZONE_RADIUS = 0.1, NEAR_ZONE_PROGRESS_BOOST = 20.0, near-zone boost branch). ruff clean, 9/9 tests pass.

| 6 | 2026-08-25 | reach_reward.py: CLOSENESS_COEFFICIENT 2.0 -> 4.0 (structure unchanged) | Exp 5 left the median at 3.9 cm, just outside the 3 cm threshold, with failures concentrated in the 4-6 cm near-miss band. In that band the exponential term contributes ~15/m of gradient at C=2 (on top of the flat 10/m linear term); doubling C to 4.0 doubles it to ~30/m at 5 cm and ~44/m at 3 cm, sharpening value discrimination exactly where failures concentrate. The term stays potential-based (zero kinks — exp 4 lesson) and its total magnitude remains modest: ~1.4 extra return for a full 8->3 cm approach vs the 5.0 bonus, so exp 3's sparse/dense balance is preserved. Expect higher success rate and lower median distance via more near-misses converted. | 48 | 6.7 | 3.7 | kept |

Diff summary (exp 6, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (CLOSENESS_COEFFICIENT = 4.0).

Note (exp 6): hypothesis confirmed — new best, 44% -> 48% overall and median distance 3.9 -> 3.7 cm (mean 7.5 -> 6.7). Training was again clean and better than exp 5: rollout success rose monotonically from ~0.25 to 0.61-0.63 (exp 5 peaked at 0.56) with no late-run decay, ep_rew_mean crossed zero (+0.35 by the end vs -0.59), explained_variance 0.85-0.91 late. The doubled near-target gradient converted more near-misses without destabilizing anything. Median now sits 0.7 cm above threshold; remaining gap is still final-band precision plus a far-miss tail (worst 21.8 cm). Next candidates: continue scaling C (e.g. 8.0 — same smooth structure, gradient ~60/m at 5 cm, max extra return ~2.8 still below bonus), shorten CLOSENESS_LENGTH_SCALE (e.g. 0.03) for sharper focus on the last centimeters, or lower constant LR to consolidate precision given the stable curve.

| 7 | 2026-08-25 | reach_reward.py: CLOSENESS_COEFFICIENT 4.0 -> 8.0 (structure unchanged) | The 4-6 cm near-miss band still holds most failures (median 3.7 cm vs 3 cm threshold; far tail worst 21.8 cm). Doubling C has now worked twice in a row (+4 pts each: exps 5, 6) with consistently healthier training curves — at C=8 the exponential term adds ~59/m of gradient at 5 cm and ~88/m at 3 cm (vs ~29/m and ~44/m), sharpening value discrimination exactly where failures concentrate. The term stays potential-based (zero kinks, exp 4 lesson) and its total magnitude remains bounded: ~2.8 extra return for a full 8->3 cm approach vs the 5.0 bonus, so exp 3's sparse/dense balance is preserved. Expect higher success rate and lower median distance via more near-misses converted. | 53 | 6.7 | 3.0 | kept |
| 8 | 2026-08-25 | train.py: PPO_HYPERPARAMETERS += ent_coef 0.01 (SB3 default 0.0; no other change) | Exp 7 identified the far-miss tail as the binding constraint (median already AT the 3 cm threshold; mean 6.7 dragged by episodes that barely approach; worst 21.8 cm) and showed policy std decaying 1.0 -> ~0.81 — premature exploration collapse plausibly locks the policy onto bad approach directions for some targets and never recovers within 30k steps. With a diagonal-Gaussian policy the entropy bonus scales with sum(log std), so ent_coef=0.01 adds gentle per-update pressure against std collapse, keeping stochastic exploration alive long enough to escape those locked-in approaches, while remaining far too weak to prevent exploitation of the strong near-target gradient (C=8 closeness + linear progress still dominate any plausible return difference). This is also a deliberate switch of lever class (exploration) instead of a fourth consecutive closeness-coefficient tweak — pushing C to 16 would make the closeness swing (~5.6 return) exceed the 5.0 success bonus and break exp 3's tuned sparse/dense balance. Expect higher success rate (tail conversion) and lower MEAN distance; median should hold at or below threshold. | 55 | 6.9 | 2.9 | kept |

Diff summary (exp 8, filled after edit, before training): `robot_learning/train.py | 1 +` (added `"ent_coef": 0.01` to PPO_HYPERPARAMETERS).

Diff summary (exp 7, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (CLOSENESS_COEFFICIENT = 8.0).

Note (exp 7): hypothesis confirmed — new best, 48% -> 53% and median distance 3.7 -> 3.0 cm (mean unchanged at 6.7; worst 21.8 cm). Healthiest training run yet: rollout success climbed monotonically to 0.67 by the end with no plateau or decay (exp 6: 0.61-0.63), ep_rew_mean reached +2.66, explained_variance 0.78-0.86 late, and ep_len_mean dropped to ~102 (faster terminations). The median now sits exactly at the threshold — half of all episodes succeed — but the far-miss tail (mean 6.7 = median 3.0) is now clearly the binding constraint: a minority of episodes barely approach at all. Next candidates: continue scaling C (e.g. 16.0 — max extra return ~5.6 approaches the 5.0 bonus, so watch sparse/dense balance), add a small per-step dwell/hold term inside threshold to convert borderline arrivals, or attack the tail directly with exploration (ent_coef) since std decayed to ~0.81 and the tail may reflect premature policy collapse on hard targets.

Note (exp 4): hypothesis decisively falsified — eval collapsed to 7% / median 13.0 cm (worse than even the 6% baseline), despite training-rollout success_rate looking healthy (0.31-0.43 from 14k-27k). The boost as implemented gates on the step's ENDING distance, so it introduces an asymmetric discontinuity at 0.10 m (crossing inward pays 30/m on the whole step; slipping outward back over the boundary costs only -10/m), and it triples both gains AND losses inside the zone where motion is noisiest. That kink sits exactly in the failure band and plausibly poisoned advantage estimates there: rollout success decayed again late-run (0.43 @21k -> 0.31 @30k) and the final deterministic policy was near-degenerate at eval. Lessons: (a) reward discontinuities away from the success boundary are dangerous — any reshaping should be continuous in distance; (b) a smooth potential-based transform (e.g. Phi(d) = -k*d^2 or exponentially saturating closeness) would scale the gradient without a boundary jump; (c) policy std decayed 1.0 -> 0.78 with entropy never tuned — ent_coef remains an untried lever if shaping keeps failing.

Note (exp 8): hypothesis partially confirmed — new best, 53% -> 55% and median 3.0 -> 2.9 cm (now BELOW threshold), though mean rose slightly (6.7 -> 6.9, worst 25.9). Training was the strongest yet: rollout success climbed monotonically to 0.71 (exp 7: 0.67) with no plateau or decay, ep_rew_mean reached +3.3 (exp 7: +2.66), explained_variance 0.75-0.88 late, ep_len_mean dropped to ~95-97 (faster terminations than exp 7's ~102), and policy std held at ~0.887 vs ~0.81 in exp 7 — the entropy pressure did exactly what it was designed to do (prevent exploration collapse) without destabilizing exploitation. The gain is modest (+2 pts) and the far tail persists (worst 25.9 cm), so exploration alone does not cure hard targets; but the curve was still rising at 30k and std never collapsed. Next candidates: raise ent_coef to 0.02 (same lever, stronger pressure — std still decayed 1.0 -> 0.89), or attack the tail structurally via reward: a per-step dwell/hold bonus inside threshold to convert borderline arrivals.

| 9 | 2026-08-25 | train.py: PPO_HYPERPARAMETERS += n_epochs 20 (SB3 default 10; no other change) | Exp 8 ended with rollout success still CLIMBING at the 30k cap (0.71, monotone since ~6k): the policy is visibly under-converged when training stops, so the binding constraint is now optimization budget rather than reward shape or exploration. With n_steps=1024 the fixed 30k-step budget yields only ~29 PPO updates x 10 epochs of gradient work; doubling epochs to 20 doubles the effective gradient steps (~580 vs ~290 minibatch updates) WITHOUT touching the task definition, seeds, timesteps, or the tuned sparse/dense reward balance from exps 3-7 (unlike pushing CLOSENESS_COEFFICIENT to 16, whose swing would exceed the 5.0 bonus). PPO clipping bounds damage from extra passes over each rollout, and exp 8 showed healthy value fitting (explained_variance up to 0.88), so there is headroom for harder optimization. Expect higher success rate and lower mean/median distance via faster convergence and more far-tail conversion before the budget expires. | 50 | 6.9 | 3.1 | reverted (worse) |

Diff summary (exp 9, filled after edit, before training): `robot_learning/train.py | 1 +` (added `"n_epochs": 20` to PPO_HYPERPARAMETERS).

| 10 | 2026-08-25 | train.py: PPO ent_coef 0.01 -> 0.02 (no other change) | Dose-response continuation of the one confirmed anti-tail lever: exp 8 showed ent_coef=0.01 held policy std at 0.887 (vs 0.81 collapse without it) and gained +2 pts, but std STILL decayed 1.0 -> 0.89, so the pressure was not saturated while the gross far-miss tail persisted (worst 25.9 cm, typical failure ends ~13 cm away = barely approached). These gross failures are structurally the hard configurations: every reset starts fully extended at a kinematic singularity, and targets behind the arm (initial gap up to ~42 cm) require committing to an elbow-reconfiguration branch through that singularity — exactly the situations where premature exploitation locks a wrong approach. Doubling the entropy coefficient doubles per-update pressure against std collapse, keeping stochastic search alive longer over branch decisions, while staying small relative to the strong shaped advantage signal (C=8 closeness + linear progress), so terminal precision should be preserved (eval is deterministic anyway). Expect higher success rate and lower MEAN distance; watch median for any precision cost. | 28 | 8.0 | 5.3 | reverted (worse) |

Diff summary (exp 10, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"ent_coef": 0.01` -> `"ent_coef": 0.02`).

Note (exp 10): hypothesis decisively falsified — eval collapsed to 28% (vs best 55%), median 2.9 -> 5.3 cm, mean 6.9 -> 8.0, and eval episode length ballooned to 153/200 (sloppy, slow approaches). Training curves looked superficially fine (explained_variance up to 0.89, rollout success climbing to 0.57 by 30k) but those stochastic-rollout metrics are confounded by the doubled noise itself (std ended 0.916 vs exp 8's 0.887; ep_rew_mean only +0.5 vs +3.3). Conclusion: ent_coef=0.01 sits close to the optimum of this lever — doubling it prevents the deterministic mean action from ever sharpening committed approaches; exploration pressure is a peaked resource here, not monotone in dose. Remaining untried lever classes: credit assignment (gamma/gae_lambda), network capacity (net_arch), reward structure aimed at the hard-configuration tail (e.g. PROGRESS_COEFFICIENT scale-up, accepting sparse/dense rebalancing risk).

Diff summary (exp 10, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"ent_coef": 0.01` -> `"ent_coef": 0.02`).

Note (exp 9): hypothesis falsified — eval dropped to 50% (vs 55%), median 2.9 -> 3.1 cm (mean unchanged 6.9, worst 20.9). Doubling epochs DID accelerate early convergence exactly as predicted (rollout success hit 0.51 by 15k and ep_rew_mean crossed zero by 16k — both far ahead of exp 8's pace), but the run then showed a NEW failure mode: success plateaued at ~0.68 from 23k and decayed to 0.63 by the end (ep_rew_mean peaked 3.6 @ 25k -> 2.53 final), and policy std collapsed harder (1.0 -> 0.80 vs exp 8's 0.887). More gradient passes over each stale rollout apparently overfit the value/policy mid-run and locked exploration down early, yielding a slightly worse deterministic policy despite the faster start. Conclusion: optimization intensity is not the binding constraint either — n_epochs stays at SB3's default 10. Remaining untried lever classes: credit assignment (gamma below 0.99 to match the ~100-step episodes, or gae_lambda), network capacity (POLICY_KWARGS net_arch beyond [64, 64]), and reward structure targeting the far tail directly (e.g. PROGRESS_COEFFICIENT scale-up, accepting the energy tax as the noise guard).

| 11 | 2026-08-25 | reach_reward.py: CLOSENESS_LENGTH_SCALE 0.05 -> 0.08 m (structure unchanged) | Failures concentrate in a mid/far band (mean 6.9 vs median 2.9; typical gross failure ends ~13 cm out, worst ~26 cm): targets behind the arm start up to ~42 cm away and demand a committed elbow reconfiguration through the fully-extended reset singularity. With lambda=0.05 the exponential term supplies almost no extra gradient beyond ~10 cm (at 12 cm it adds ~14.5/m on top of the flat 10/m linear term, at 18 cm only ~4.4/m), so exactly the hard far-tail approaches are shaped by the weakest signal — plausibly why they stall instead of committing. Broadening lambda to 0.08 keeps the same smooth potential-based structure with zero kinks (exp 4 lesson) and essentially preserves the near-miss band (gradient at 5 cm: 59 -> 53.5/m; at 6 cm: 48 -> 47.7/m) while reinforcing the mid-range where failures live (at 8 cm: 16.6 -> 36.8/m; at 12 cm: 14.5 -> 22.3/m; at 18 cm: 4.4 -> 10.5/m). Typical 8->3 cm potential swing shrinks slightly (2.78 -> 2.55), preserving exp 3's sparse/dense balance in the success region; worst-case full-workspace swing grows from ~4.4 to ~5.4 return — comparable to the 5.0 bonus, but only across rare very-long approaches, and the flat linear term still dominates far-field totals. Expect higher success rate via tail conversion and lower MEAN distance; watch that median precision holds. | 9 | 11.2 | 11.2 | reverted (worse) |

Diff summary (exp 11, filled after edit, before training): `robot_learning/rewards/reach_reward.py | 2 +-` (CLOSENESS_LENGTH_SCALE = 0.08).

Note (exp 11): hypothesis decisively falsified — eval collapsed to 9% (vs best 55%), median 11.2 cm = mean 11.2 cm, worst 22.8, eval episode length 183/200 (episodes mostly run to truncation without reaching). Same stochastic-vs-deterministic dissociation as exps 4 and 10: training rollouts looked alive (success climbing monotonically to 0.49 by 30k, ep_rew_mean rising -4.9 -> -0.48, std held at 0.845) but the final deterministic policy barely approaches. The tell was value fitting: explained_variance stayed erratic all run (0.00-0.87, frequently <0.5) vs exp 8's consistent 0.75-0.88, and ep_len_mean only fell to ~136 vs exp 8's ~95. Plausible mechanism: broadening lambda stretches the SAME potential swing across many more steps — per-step marginal gain for committing to a reconfiguration shrinks and smears out, while cumulative potential magnitude inflates the state-value range the regressor must fit (and the worst-case swing ~5.4 now rivals the 5.0 bonus, eroding exp 3's sparse/dense separation on long approaches). Lesson: the sharp lambda=0.05 focus is load-bearing — concentrating the shaping gradient near the target (where termination actually happens) matters more than extending support outward; mid-range tail failures are NOT fixable by stretching this term. Remaining untried lever classes: credit assignment (gamma/gae_lambda), network capacity (net_arch), PROGRESS_COEFFICIENT scale-up; ent_coef fine-tuning DOWNWARD (e.g. 0.005) is also open since 0.01 -> 0.02 collapsed hard (peaked resource).

| 12 | 2026-08-25 | train.py: PPO gae_lambda 0.95 -> 0.98 (no other change) | The far-miss tail (mean 6.9 vs median 2.9; gross failures stall ~13-26 cm out on targets behind the arm) requires a committed elbow-reconfiguration through the fully-extended reset singularity, where immediate dense progress is temporarily NEGATIVE — so the advantage for STARTING the maneuver must come from the future payoff (approach + success bonus). In GAE that payoff reaches decision t attenuated by (gamma*lambda)^k per step k of distance: with gamma=0.99, lambda=0.95 the factor is 0.9405^100 ~= 0.002 for a payoff 100 steps ahead — effectively ZERO, so early commitment decisions in long episodes see only the local negative-progress signal and rationally refuse to commit; this is exactly the untried credit-assignment lever class flagged by exps 2/9/10/11. Raising lambda to 0.98 raises the same 100-step propagation to 0.9702^100 ~= 0.049 (~20x stronger reach) with LESS bias, letting terminal payoffs reinforce the maneuver start. Unlike a gamma change, lambda leaves the optimization objective (returns, value scale, sparse/dense balance from exp 3) completely untouched — it only improves the advantage ESTIMATOR, and n_steps=1024 (~10 episodes/update, bootstrapped across dones) bounds the added variance. Expect higher success rate via far-tail conversion and lower MEAN distance; watch explained_variance for variance-induced degradation (exp 11 showed erratic EV predicts failure even when rollout metrics look alive). | 32 | 9.7 | 4.5 | reverted (worse) |

Diff summary (exp 12, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"gae_lambda": 0.95` -> `"gae_lambda": 0.98`).

Note (exp 12): hypothesis decisively falsified — eval collapsed to 32% (vs best 55%), median 2.9 -> 4.5 cm, mean 6.9 -> 9.7, worst 40.5, eval episode length 144/200. The training run showed the exact warning sign the hypothesis flagged to watch: explained_variance was erratic ALL run (frequently <0.5, repeatedly negative mid-run: -0.561 @ 4k, -0.265/-0.195 @ 11-12k, 0.137 @ 18k; late oscillating 0.43-0.92) vs exp 8's consistent late 0.75-0.88, and every other training metric came in below exp 8 (final rollout success 0.65 vs 0.71, ep_rew_mean +2.0 vs +3.3, std 0.871 vs 0.887, ep_len_mean ~113 vs ~95-97) despite the same monotone-climb shape. Mechanism: this task's per-step reward is noisy (stochastic-policy torque + quadratic action cost on top of dense progress), and lambda=0.98 lets those noisy TD errors from ~50-100 steps away contribute to nearly every advantage estimate — the ~20x gain in credit REACH applied equally to noise, so advantage variance swamped the bias reduction and the value regressor faced a much wider effective target distribution (approaching Monte-Carlo returns over ~150-step episodes), reproducing the stochastic-vs-deterministic dissociation of exps 4/10/11 (rollouts looked alive; deterministic eval degraded). Lesson stacked on exps 4/10/11: credit-propagation reach is ALSO a peaked resource here — gamma*lambda=0.94 (the exp 8 config) appears near the sweet spot for this noise level; do not revisit lambda increases without first reducing per-step reward noise. Remaining untried lever classes: network capacity (net_arch beyond [64,64]), PROGRESS_COEFFICIENT scale-up (accepting sparse/dense rebalancing risk), ent_coef fine-tuning DOWNWARD (e.g. 0.005).
