# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent - | Fresh baseline | unmeasured | - | invalid; researcher changes preserved |
| 2 | Fresh baseline / parent fresh | Fresh baseline | 5 measured checkpoints; 19 unmeasured checkpoints; research_evaluation/research_evaluation: 6 measurements; task_reference/task-reference-v1: 1 measurement | The baseline-establishment hypothesis is supported: a trainable policy was produced and measured. The type-specific question of the measurement rounds, whether the restricted-radius fresh baseline generalizes to the full official 0.06-0.20 m distribution and whether a saved checkpoint reaches the 98% objective, is partially supported. Generalization across the radius range largely holds (no radial gradient for the leader), and the leading checkpoint reached 97.5% on one panel and 98.0% on the reused task-reference panel. However, the round-2 prediction that the leader would remain at or above 98% on a disjoint panel is contradicted: it scored 95.5% there, and the pooled research estimate is 96.2%, below the objective. No measured checkpoint convincingly satisfies the 98% criterion. Limits: only five of 24 checkpoints measured, two disjoint research panels, one training seed and recipe, and the reused task-reference panel is selection-contaminated and not privileged. The localized sector finding is descriptive and not causally attributed. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 3 | Set TRAINING_TARGET_RADIUS_RANGE in robot_learning/scenario/training_environment.py from (0.14, 0.20) to the full official (0.06, 0.20) m and fine-tune the working policy on that distribution, leaving task mechanics, observation construction and the saved action contract unchanged. / parent working | Set TRAINING_TARGET_RADIUS_RANGE in robot_learning/scenario/training_environment.py from (0.14, 0.20) to the full official (0.06, 0.20) m and fine-tune the working policy on that distribution, leaving task mechanics, observation construction and the saved action contract unchanged.; files: robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted. The confirmatory prediction was that full-range fine-tuning would lift measured success above the 96.2% pooled estimate toward 98%, with fewer failures at angles about −117 to −155 degrees and radii below 14 cm. Instead all three policies landed within 0.34 points of each other, the untouched parent was marginally best, and the same folded-branch angles failed at both small and large radii. The pre-stated alternative — a control/representation limitation at the shoulder-limit folded-elbow branch rather than a restricted-radius coverage artifact — is supported by this test. Limits: one fine-tuning run and one 600-episode panel; the two measured fine-tuned checkpoints are the run's endpoint and training-proxy extreme, so an unmeasured mid-run checkpoint is not covered; the folded-branch attribution remains a kinematic feasibility computation from the joint limits, not a causal diagnosis of the policy's internal state; the small success-rate differences (≤3 discordant episodes of 600) are within panel resolution. | working working; code keep |
| 4 | Reshape the scenario reward in robot_learning/scenario/reward.py to sharpen terminal precision and hold completion: CLOSENESS_LENGTH_SCALE 0.05 -> 0.015 m, HOLD_PROGRESS_EXPONENT 1.0 -> 2.0, HOLD_EXIT_FORFEIT_FRACTION 0.0 -> 1.0. Observation construction, action mapping, task mechanics and success semantics are unchanged. Train from the working policy with transfer initialization for the standard 120,000-step budget on the unchanged full 0.06-0.20 m target distribution. / parent working | Reshape the scenario reward in robot_learning/scenario/reward.py to sharpen terminal precision and hold completion: CLOSENESS_LENGTH_SCALE 0.05 -> 0.015 m, HOLD_PROGRESS_EXPONENT 1.0 -> 2.0, HOLD_EXIT_FORFEIT_FRACTION 0.0 -> 1.0. Observation construction, action mapping, task mechanics and success semantics are unchanged. Train from the working policy with transfer initialization for the standard 120,000-step budget on the unchanged full 0.06-0.20 m target distribution.; files: robot_learning/scenario/reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted. The confirmatory prediction was that sharpening terminal precision and hold completion (CLOSENESS_LENGTH_SCALE 0.05 -> 0.015 m, HOLD_PROGRESS_EXPONENT 1.0 -> 2.0, HOLD_EXIT_FORFEIT_FRACTION 0.0 -> 1.0) would reduce the folded-branch terminal failures and raise measured success above the parent's level. Instead the reshaped-reward checkpoints measured clearly below the untouched parent, the folded-branch band was unchanged (36/47 vs 37/47), and hold precision degraded in bands the parent handles at 100%, with far more reached-then-lost failures. Limits: only the run's training-proxy plateau extremes (checkpoint-100352 and checkpoint-105472) were measured of 24 candidates; one 600-episode panel; the transfer perturbed then recovered, so an unmeasured early checkpoint is not covered; the small remaining differences are within panel resolution. | working working; code revert |
| 5 | In robot_learning/scenario/observations.py, condition the open-branch IK error on joint-limit feasibility: when the open-elbow solution requires a shoulder angle outside the +/-170 degree hinge range, expose the reachable folded-elbow configuration in the open-branch slots instead. The 11-dimensional observation layout, the qpos/qvel/end-effector features, and the folded-branch pair are unchanged; no reward, task mechanics, or success semantics change. / parent working | In robot_learning/scenario/observations.py, condition the open-branch IK error on joint-limit feasibility: when the open-elbow solution requires a shoulder angle outside the +/-170 degree hinge range, expose the reachable folded-elbow configuration in the open-branch slots instead. The 11-dimensional observation layout, the qpos/qvel/end-effector features, and the folded-branch pair are unchanged; no reward, task mechanics, or success semantics change.; files: robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted. The diagnostic proposition was that exposing the reachable folded configuration in the open-branch slots would reduce the folded-required failure rate of the working policy while leaving open-feasible behavior unchanged. The secondary clause held exactly (open-feasible stayed 567/567 = 100% for both checkpoints), but the primary proposition is contradicted: folded-required measured success fell from 54.5% (18/33) to 0% (0/33), and the parent won every discordant episode. Limits: one transfer run and training seed, one fresh 600-episode panel, and only the two training-proxy plateau extremes (checkpoint-100352 and checkpoint-120832) of 24 candidates. The two measured checkpoints are behaviorally identical on this panel (0 discordant of 600), so a different late-plateau checkpoint would not be expected to differ; an unmeasured early checkpoint is not covered. The folded/open partition is a kinematic feasibility computation from target geometry and joint limits, not a joint-trajectory or limit-contact observation. | working working; code revert |
| 6 | In robot_learning/scenario/observations.py, project the open-branch IK shoulder target onto the arm's +/-170 degree hinge range before forming the open-branch shoulder error, so an out-of-range open solution is represented by the nearest reachable shoulder angle instead of an angle that wraps to within a few degrees of +/-180. The 11-dimensional layout, the qpos/qvel/end-effector features, the elbow error, the folded-branch pair, the reward, task mechanics and success semantics are unchanged. / parent working | In robot_learning/scenario/observations.py, project the open-branch IK shoulder target onto the arm's +/-170 degree hinge range before forming the open-branch shoulder error, so an out-of-range open solution is represented by the nearest reachable shoulder angle instead of an angle that wraps to within a few degrees of +/-180. The 11-dimensional layout, the qpos/qvel/end-effector features, the elbow error, the folded-branch pair, the reward, task mechanics and success semantics are unchanged.; files: robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted. The diagnostic proposition was that projecting the wrapped open shoulder onto the +/-170 degree range would recover folded-required success, move the previously-failing 176-180 degree band toward the shallow-sector level, and leave open-feasible success at 100%. Both stated contradicting conditions occurred: the 176-180 degree band stayed at 0/15, folded-required success overall fell (66.0% -> 44.0%/48.0%), and open-feasible success fell below 100% (950/950 -> 944/950 and 942/950). The pre-stated alternative - an intrinsic precision or optimization limit of the 64x64 policy in the folded configuration rather than the wrap seam - is consistent with this result, and the collateral open-feasible failures are an additional unexpected degradation the seam explanation does not predict. Limits: one transfer run and one training seed; one fresh 1000-episode panel; only the two training-proxy plateau extremes (`checkpoint-100352` and `checkpoint-120832`) of 24 candidates were measured, though late-plateau behavior has been homogeneous in prior runs and the two agree 8 vs 8 here; without an unchanged-continuation control the run cannot separate the representation change from ordinary weight drift under continued training; the folded/open partition and the seam bands are kinematic computations from recorded target geometry, not observations of the policy's internal state. | working working; code revert |
