# Experiment log

| # | Date | Operation | Hypothesis | Candidate success | Seeds passed | Verdict |
|---:|---|---|---|---:|---:|---|
| 1 | 2026-09-04 | Fresh baseline | Establish the initial baseline for the human-defined objective. | 97.0 | - | measured as requested; awaiting researcher analysis |
| 2 | 2026-09-04 | Replace the far-target-only training radius distribution with the full official 6-20 cm radius range while preserving the PPO method, angular distribution, robot, reward, and evaluation panel. If short-radius exposure is not the limiting mechanism, the intervention should not materially reduce the repeated short-radius failures relative to the fresh baseline. | Sampling training targets uniformly across the full 6-20 cm radius range, instead of only 14-20 cm, will reduce the baseline's repeatable short-radius non-reach failures in the negative-angle sector and improve fixed-panel success above 97% without increasing hold interruptions. | 59.5 | - | measured as requested; awaiting researcher analysis |
| 3 | 2026-09-04 | Augment the scenario observation with target radius and sine/cosine target-angle features, preserving the existing robot, reward, target-radius training distribution, PPO configuration, and evaluation semantics. This tests whether the baseline's narrow negative-angle blind spot is caused by an inconvenient implicit Cartesian/IK representation rather than insufficient short-radius sampling. | Adding explicit continuous target polar features (radius, sine of target angle, and cosine of target angle) to the policy observation will reduce the baseline's repeatable negative-angle non-reach failures and improve fixed-panel success above 97% without changing the far-target-focused training distribution or increasing hold interruptions. | 25.5 | - | measured as requested; awaiting researcher analysis |
| 4 | 2026-09-04 | Add a training-only 50% mixture component for target angles in [-2.7, -1.9] radians, preserving the uniform full-angle component, 14-20 cm training radius, robot, reward, PPO configuration, and evaluation panel. The hypothesis predicts fewer failures near -122° to -145°; if success remains near baseline with the same angular failures, the alternative explanation is that the blind spot is caused by policy representation or control dynamics rather than insufficient exposure. | Keeping the baseline far-target radius distribution but sampling half of training targets from the repeated failure band [-155°, -109°] will reduce the baseline's reproducible negative-angle non-reaches and raise fixed-panel success above 97%. | 82.0 | - | measured as requested; awaiting researcher analysis |
| 5 | 2026-09-04 | Keep the baseline robot, observations, reward, target distribution, network and training budget, but set PPO gamma to 0.995 so delayed hold completion contributes more strongly to policy updates. | Increasing PPO gamma from 0.99 to 0.995 will improve credit assignment across the 100-step hold, reducing the late-training hold interruptions seen in the 97% baseline while preserving its reach performance. If the remaining failures are primarily angular non-reaches rather than temporal-credit failures, success will remain near or below the baseline and the failure mix will not improve. | 22.5 | - | measured as requested; awaiting researcher analysis |
| 6 | 2026-09-04 | Replicate the current method from fresh initialization | The unchanged PPO baseline's 97% result is reproducible across training seeds rather than a seed-0 accident; a fresh 120,000-step run with training seed 1 will achieve comparable performance and failure patterns. The plausible alternative is seed sensitivity, shown by a materially different result, especially a result at or above the 98% objective threshold. Comparing this run with experiment 1 on the same development measurements distinguishes these explanations. | 44.5 | - | measured as requested; awaiting researcher analysis |
| 7 | 2026-09-04 | Increase training parallelism from one environment to four while preserving the total PPO rollout size at 1024 transitions; keep the robot, target distribution, observations, reward, policy network, PPO hyperparameters, and 120000-step budget unchanged. | Using four parallel training environments while preserving PPO's total 1024-transition rollout will reduce seed-sensitive gradient noise and allow the failing training seed 1 to learn the reach-and-hold policy more reliably, improving fixed-panel success substantially above experiment 6's 44.5% and toward the accepted 97.0% baseline. If the instability is instead caused by a seed-specific optimization basin that parallel rollouts do not address, performance will remain near the experiment 6 result. | 15.5 | - | measured as requested; awaiting researcher analysis |
| 8 | 2026-09-04 | Enable PPO generalized state-dependent exploration (use_sde=true) and resample its exploration matrix every 64 control steps (sde_sample_freq=64), while retaining the accepted one-environment PPO configuration and training seed 1 for a direct comparison with experiment 6. | The seed-1 baseline failure is caused in part by temporally unstructured action noise preventing PPO from discovering stable reach trajectories. Enabling PPO state-dependent exploration with noise held for 64 control steps should improve the 120,000-step seed-1 policy beyond the 44.5% research result of experiment 6 and reduce non-reach failures without changing the task, reward, observations, or policy architecture. The alternative is that the failure is primarily an unfavorable optimization basin unrelated to action-noise temporal structure; in that case gSDE will remain at or below the experiment-6 baseline and may not preserve the hold. | 0.0 | - | measured as requested; awaiting researcher analysis |
| 9 | 2026-09-04 | Set the researcher-owned reward parameter HOLD_EXIT_FORFEIT_FRACTION from 0.0 to 1.0, so losing tolerance forfeits the accumulated hold-progress capital while preserving the existing progress, closeness, outside-band, completion, action-cost terms, task mechanics, observations, target distribution, and PPO configuration. | Forfeiting all accumulated hold-progress reward when the end effector exits tolerance will teach PPO to preserve the 2-second hold, reducing the accepted baseline's late hold interruptions and improving fixed-panel success above 97% without harming reach performance. The plausible alternative is that the remaining failures are primarily angular non-reaches or seed-dependent optimization failures; under that explanation, the intervention will leave non-reach counts and overall success near the baseline. | 21.5 | - | measured as requested; awaiting researcher analysis |
