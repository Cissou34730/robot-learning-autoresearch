# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 3 measurements | Partially supported. As an automatic fresh baseline, the experiment established that the unchanged method can learn a policy with high measured task performance and identified a useful checkpoint near 100,352 steps. It did not establish that the learned policy reliably satisfies the 98% objective: the strongest independent measurement was 97.0%, and the task-reference result is development evidence on a distinct fixed panel. The non-monotonic late training metrics and the lower independent-panel results weaken any inference that continuing this baseline recipe to the final checkpoint improves the task policy. These conclusions concern the saved checkpoints and measured panels, not unmeasured checkpoints or the official final panel. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Set HOLD_EXIT_FORFEIT_FRACTION from 0.0 to 1.0 so leaving the success band forfeits the accumulated hold-progress capital. / parent working | Set HOLD_EXIT_FORFEIT_FRACTION from 0.0 to 1.0 so leaving the success band forfeits the accumulated hold-progress capital.; files: robot_learning/scenario/reward.py, tests/scenario/test_reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 2 measurements | Weakened. The expected improvement in independent-panel success and reduction of negative-angle failures did not occur at the strongest measured experiment-2 checkpoint, although that checkpoint preserved the parent's independent-panel outcomes and most of the previously successful task-reference behavior. The later checkpoint showed broad regression rather than a focused hold improvement. This weakens the hold-continuity explanation under the tested transferred recipe and leaves geometry-specific control or representation limits as plausible alternatives; it does not identify a causal mechanism because this was one changed-reward training run and checkpoints also differ in training time. | working working; code revert |
| 3 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 3 measurements | Contradicted under the tested fresh seed and measured checkpoints. The expected near-threshold result at a checkpoint near 100,000 steps did not occur on either development panel; the observed lower performance supports the proposal's alternative that the experiment-1 result can be seed- or checkpoint-specific. The assessment is limited to one replication seed and the three measured checkpoints: the 21 unmeasured checkpoints remain unmeasured, and this result does not estimate population-level seed variance. | working working; code keep |
