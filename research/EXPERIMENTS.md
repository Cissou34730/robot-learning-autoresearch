# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 5 measurements; task_reference/task-reference-v1: 3 measurements | Partially supported. The baseline established that the method can learn substantial reach-and-hold behavior, and late training proxies corresponded to better measured policies than the reward-peak intermediate checkpoint. However, no independent development measurement reached the 98% objective, and the fixed-panel 98% result cannot establish that objective because it was used in selection. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change the training-only target sampler to cover radii from 6-20 cm and use a 50% mixture that samples the observed -180 to -90 degree angular sector and a 50% full-angle draw, while leaving evaluation and official task mechanics unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change the training-only target sampler to cover radii from 6-20 cm and use a 50% mixture that samples the observed -180 to -90 degree angular sector and a 50% full-angle draw, while leaving evaluation and official task mechanics unchanged.; files: robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted under the tested transfer recipe. The predicted improvement from broader radii and focused angular exposure was absent at both measured transfer checkpoints, with a small decrease relative to the contemporaneous control. This is evidence against this specific continuation and sampler mixture, not proof that every target-coverage intervention would fail. | working working; code revert |
| 3 | Change the training-only target radius sampler from 14-20 cm to a uniform 6-20 cm range; leave the full-angle sampler, task mechanics, evaluation, and official success definition unchanged. / parent fresh | Change the training-only target radius sampler from 14-20 cm to a uniform 6-20 cm range; leave the full-angle sampler, task mechanics, evaluation, and official success definition unchanged.; files: robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted under the tested fresh uniform-full-radius recipe. The predicted improvement over the narrow-radius policy was absent; both measured challengers were 23.75 to 25.625 percentage points worse than the contemporaneous control, and the final checkpoint was slightly worse than the matched checkpoint. This rejects the tested recipe as the next development direction, but does not prove that every fresh full-range curriculum or target-coverage intervention will fail. | working working; code revert |
| 4 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change only the training-only target-angle sampler: retain the 14-20 cm radius range and draw the -180 to -90 degree sector with probability 0.5, otherwise draw a full-angle target; leave evaluation, task mechanics, and the official success definition unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change only the training-only target-angle sampler: retain the 14-20 cm radius range and draw the -180 to -90 degree sector with probability 0.5, otherwise draw a full-angle target; leave evaluation, task mechanics, and the official success definition unchanged.; files: robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted under the tested focused-angle continuation. Oversampling the observed -180 to -90 degree sector produced no held-out gain at the early proxy checkpoint and degraded performance after the full continuation. The tied early-checkpoint score does not independently justify selecting that lineage because the same panel supplied the selection evidence; it only supports retaining the established parent. This does not rule out every intervention on angular exposure, representation, or optimization. | working working; code revert |
| 5 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change only the training reward's HOLD_EXIT_FORFEIT_FRACTION from 0.0 to 0.5, forfeiting half of accumulated hold-progress credit when the end effector leaves tolerance; leave target sampling, observations, task mechanics, evaluation, and the official success definition unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change only the training reward's HOLD_EXIT_FORFEIT_FRACTION from 0.0 to 0.5, forfeiting half of accumulated hold-progress credit when the end effector leaves tolerance; leave target sampling, observations, task mechanics, evaluation, and the official success definition unchanged.; files: robot_learning/scenario/reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted under the tested reward-modified continuation. The predicted reduction in hold interruptions and improvement in held-out success did not occur at the early proxy checkpoint or after full continuation; both candidates were materially worse than the contemporaneous control. This rejects the tested coefficient and continuation recipe, not every possible hold-sensitive reward or training intervention. | working working; code revert |
| 6 | Change only the policy MLP architecture from tanh [64, 64] to tanh [128, 128]; leave the target sampler, observations, reward, task mechanics, evaluation, and other PPO settings unchanged. / parent fresh | Change only the policy MLP architecture from tanh [64, 64] to tanh [128, 128]; leave the target sampler, observations, reward, task mechanics, evaluation, and other PPO settings unchanged.; files: research/current_params.json | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted under the tested fresh capacity-increase recipe. Neither the reward-peak nor final wider checkpoint improved held-out reach-and-hold success; both were dramatically below the contemporaneous control, so the proposed representation benefit was not observed. This rejects the tested tanh [128,128] fresh recipe, not every possible capacity, initialization, or optimization intervention. | working working; code revert |
