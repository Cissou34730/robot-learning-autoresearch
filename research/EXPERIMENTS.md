# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 5 measurements; task_reference/task-reference-v1: 3 measurements | The baseline expectation that ordinary PPO training would learn the reach-and-hold task is partially supported: success improved from near zero to roughly 96% on pooled disjoint development panels. The stronger claim that the late training peak represented a policy satisfying the human 98% objective is weakened by the pooled result below 98% and by the repeated angle-specific failures. This conclusion is limited to the measured checkpoints and development panels; the official result remains unmeasured. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Changed robot_learning/scenario/training_environment.py so continued training samples target radii uniformly across the official 0.06-0.20 m range instead of only 0.14-0.20 m. / parent working | Changed robot_learning/scenario/training_environment.py so continued training samples target radii uniformly across the official 0.06-0.20 m range instead of only 0.14-0.20 m.; files: robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Partially supported. The preservation component was supported on this panel: full-radius training did not reduce the transferred policy's measured success and retained the same binary outcomes as the parent. The central prediction of improved short negative-angle complete-task success was not supported; the intervention checkpoints retained both failures, and the additional failure above 14 cm shows that omitted short-radius coverage was not sufficient to explain the residual weakness. The result is limited to the measured candidates and one disjoint development panel. | working working; code revert |
| 3 | Change the researcher-owned reward so exiting tolerance forfeits all accumulated hold-progress potential and remaining outside after an interruption is penalized at 1.0 rather than 0.1. / parent working | Change the researcher-owned reward so exiting tolerance forfeits all accumulated hold-progress potential and remaining outside after an interruption is penalized at 1.0 rather than 0.1.; files: robot_learning/scenario/reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction was that forfeiting accumulated hold-progress after an interruption and increasing the outside-tolerance penalty would improve uninterrupted holding without sacrificing broad reach behavior. Neither measured intervention improved complete-task success or the parent’s residual pair, and both caused a large reach-and-hold regression. This conclusion is limited to the two measured checkpoints, one disjoint development panel, and the changed reward recipe; training-time logs and the official benchmark were not evidence for this assessment. | working working; code revert |
| 4 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/policy_io.py so training and saved-policy inference apply reset-safe first-order smoothing to successive joint-effort commands, using a 0.25 previous-command contribution after the first action while leaving observations and the PPO network unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/policy_io.py so training and saved-policy inference apply reset-safe first-order smoothing to successive joint-effort commands, using a 0.25 previous-command contribution after the first action while leaving observations and the PPO network unchanged.; files: robot_learning/scenario/policy_io.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction was that a 0.25 previous-command contribution would reduce torque-driven overshoot and repeated tolerance interruptions without sacrificing coordinated reaching. Both measured smoothed policies instead lost broad complete-task success relative to the same-panel parent. This conclusion is limited to the two measured checkpoints, one disjoint development panel, and this smoothing recipe; the official benchmark was not evidence for the assessment. | working working; code revert |
| 5 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/observations.py so the three target-displacement observation slots carry target radius, sine of target angle, and cosine of target angle. Keep the 11-value observation size, joint positions and velocities, and both inverse-kinematic branch residual pairs unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/observations.py so the three target-displacement observation slots carry target radius, sine of target angle, and cosine of target angle. Keep the 11-value observation size, joint positions and velocities, and both inverse-kinematic branch residual pairs unchanged.; files: robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction was that replacing redundant planar displacement with radius and continuous target-angle sine/cosine would improve difficult negative-angle reaching while preserving broad complete-task behavior. Neither measured polar policy improved the parent or produced a task-level gain; the matched policy lost seven paired successes and the later policy lost 27. The conclusion is limited to these two checkpoints and one disjoint development panel; training proxies and the official benchmark were not evidence for the human objective. | working working; code revert |
| 6 | Continue training lineage checkpoint-100352@1 with a changed recipe: Add a quadratic endpoint-speed penalty to the researcher-owned reward only when the end effector is within the success tolerance, using the Jacobian-derived endpoint speed while preserving the existing observation, action mapping, and task mechanics. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Add a quadratic endpoint-speed penalty to the researcher-owned reward only when the end effector is within the success tolerance, using the Jacobian-derived endpoint speed while preserving the existing observation, action mapping, and task mechanics.; files: robot_learning/scenario/environment.py, robot_learning/scenario/reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction was that penalizing Jacobian-derived endpoint speed inside the tolerance disk would reduce residual-motion hold interruptions without harming reaching. Neither measured intervention improved complete-task success or the parent's failures; both caused large broad regressions, including unstable holds and non-completing episodes. The conclusion is limited to these two checkpoints, one disjoint development panel, and the tested quadratic penalty recipe; it does not identify the primary dynamic cause of the working lineage's remaining failures, and the official benchmark was not evidence for the assessment. | working working; code revert |
| 7 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/training_environment.py and robot_learning/scenario/environment.py so training oversamples target angles uniformly in the empirically difficult -160 to -110 degree sector with probability 0.35, while preserving the existing radius range and leaving saved-policy inference and official evaluation mechanics unchanged. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/training_environment.py and robot_learning/scenario/environment.py so training oversamples target angles uniformly in the empirically difficult -160 to -110 degree sector with probability 0.35, while preserving the existing radius range and leaving saved-policy inference and official evaluation mechanics unchanged.; files: robot_learning/scenario/environment.py, robot_learning/scenario/training_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction that oversampling -160 to -110 degrees would improve the concentrated negative-angle failures while preserving broad behavior was not supported: the early checkpoint preserved the parent's binary outcomes without fixing any of its four failures, and the final continuation regressed broadly and introduced positive-angle failures. This conclusion is limited to the two measured intervention checkpoints, one disjoint research panel, and the tested 35% oversampling recipe; the official benchmark remains unmeasured. | working working; code revert |
| 8 | Change robot_learning/scenario/observations.py so the always-zero planar z displacement observation slot carries the signed difference between the open-branch and folded-branch joint-residual norms, while retaining the two planar displacement values, joint state, and four individual branch residuals. / parent working | Change robot_learning/scenario/observations.py so the always-zero planar z displacement observation slot carries the signed difference between the open-branch and folded-branch joint-residual norms, while retaining the two planar displacement values, joint state, and four individual branch residuals.; files: robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction that exposing the policy to a signed difference between open- and folded-branch residual norms would improve the concentrated negative-angle failures without broad regression was not supported: neither measured checkpoint improved the parent, the shared negative-angle non-reaches remained, and both checkpoints lost paired successes. This conclusion is limited to the two measured checkpoints, one disjoint research panel, and this observation representation; the unmeasured checkpoints and official benchmark do not support additional claims. | working working; code revert |
| 9 | Change robot_learning/scenario/observations.py so the third target-displacement slot carries the normalized difference between the open and folded inverse-kinematic branches' minimum joint-limit margins instead of the existing signed residual-margin feature, while retaining the planar x/y displacement, joint state, and four geometric branch residuals. / parent working | Change robot_learning/scenario/observations.py so the third target-displacement slot carries the normalized difference between the open and folded inverse-kinematic branches' minimum joint-limit margins instead of the existing signed residual-margin feature, while retaining the planar x/y displacement, joint state, and four geometric branch residuals.; files: robot_learning/scenario/evaluation.py, robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. Providing relative joint-limit clearance did not reduce the targeted negative-angle failures or preserve broad complete-task behavior; both measured checkpoints regressed substantially despite training-success proxies of 0.99 and 0.98. This conclusion is limited to the late proxy-peak and final checkpoints, one disjoint research panel, and the tested observation representation; the other candidates and official benchmark remain unmeasured. | working working; code revert |
| 10 | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/policy_io.py so the training and saved-policy inference action map applies a 0.65 physical-effort scale to each instantaneous joint command without temporal smoothing. Extend robot_learning/scenario/evaluation.py to record applied effort and closest joint-state diagnostics needed to distinguish overshoot and joint-limit changes from complete-task success. / parent working | Continue training lineage checkpoint-100352@1 with a changed recipe: Change robot_learning/scenario/policy_io.py so the training and saved-policy inference action map applies a 0.65 physical-effort scale to each instantaneous joint command without temporal smoothing. Extend robot_learning/scenario/evaluation.py to record applied effort and closest joint-state diagnostics needed to distinguish overshoot and joint-limit changes from complete-task success.; files: robot_learning/scenario/evaluation.py, robot_learning/scenario/policy_io.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction that scaling instantaneous physical effort to 0.65 without temporal smoothing would reduce the working lineage's negative-angle non-reaches and interrupted holds without broad regression was not supported. The matched and final candidates both lost substantial complete-task success to working, and continued adaptation under the changed action map worsened both reach and hold outcomes. This conclusion is limited to the two measured effort-scaled checkpoints, one fresh research panel, and this action-map recipe; it does not establish that every lower-effort controller or every approach intervention will fail, and the official benchmark remains unmeasured. | working working; code revert |
| 11 | Keep the current observation, reward, task, and action mapping unchanged, but override PPO with a lower learning rate, a narrower clip range, and no entropy bonus so continued learning makes smaller policy changes around the retained working behavior. / parent working | ppo.learning_rate: 0.0003 -> 5e-05; ppo.clip_range: None -> 0.05; ppo.ent_coef: 0.01 -> 0.0 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Contradicted on the measured panel. The prediction that a lower learning rate, narrower PPO clipping range, and zero entropy bonus would refine residual negative-angle reach-and-hold behavior while preserving broad performance was not supported: both measured candidates scored below working and both lost paired outcomes. This conclusion is limited to the two measured checkpoints, one fresh 160-episode research panel, and this PPO-update recipe; the unmeasured candidates and official benchmark do not support additional claims. | working working; best known working; code revert |
| 12 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | Partially supported. Fresh initialization exposed substantial seed-dependent optimization variance, but it did not yield the materially better policy anticipated as one possible outcome, and its failures were broader than the standing lineage's concentrated negative-angle residuals. This conclusion is limited to one unchanged fresh replication, the two measured checkpoints, and one disjoint 160-episode research panel; it does not establish the cause of the variance or classify the 22 unmeasured candidates. | working working; code keep |
