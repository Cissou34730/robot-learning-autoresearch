# Experiment log

**Best so far:** 100% (experiment 19)

## Lessons that shaped the current best (condensed from experiments 0-11)

- Exp 1 (n_steps 256, reverted): more updates alone did not help; noise-driven contact.
- Exp 2 (action cost 0.05): +8 pts. Flailing now pays; policy more directed.
- Exp 3 (SUCCESS_BONUS 25 -> 5): +26 pts. Sparse/dense scale balance stabilized value fitting — binding-constraint fix.
- Exp 4 (hard near-zone gate, reverted): discontinuity at the gate poisoned advantages. Lesson: shaping must be continuous in distance.
- Exp 5 (exponential closeness potential C=2, lambda=0.05): +4 pts, median 3.9 cm. Potential-based shaping works; healthiest training curve so far.
- Exp 6 (C=4): +4 pts, median 3.7 cm. Doubling C converted near-misses without instability.
- Exp 7 (C=8): 53%, reverted. Coefficient scaling exhausted around C=4.
- Exp 8 (ent_coef 0.01): +2 pts to 55%. Current champion; fresh-seed audit confirmed 54% on unseen targets.
- Exp 9 (n_epochs 20, reverted): faster start but mid-run overfit and exploration lock-down; optimization intensity is not the constraint.
- Exp 10 (ent_coef 0.02, reverted): collapsed hard; ent_coef peaked near 0.01.
- Exp 11 (lambda 0.08, reverted): collapsed to 9%. The sharp lambda=0.05 focus near target is load-bearing; do not stretch it outward.

## Remaining untried lever classes

gamma / gae_lambda (credit assignment), net_arch (capacity), PROGRESS_COEFFICIENT scale-up,
ent_coef fine-tune downward (0.005).

Full original notes for experiments 0-11: see `research/archive.md`.

Diff summary (exp 13, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

## Results table

| # | Date | Change | Hypothesis | Success % | Mean dist (cm) | Median dist (cm) | Verdict |
|---|------|--------|------------|-----------|----------------|------------------|---------|
| 13 | 2026-08-25 | train.py: POLICY_KWARGS net_arch [64, 64] -> [128, 128] (no other change) | Network capacity is the last untried lever class: the champion's value function plateaus at explained_variance 0.75-0.88 and deterministic eval fails precisely on behind-arm targets whose approach requires representing both IK branches around the fully-extended reset singularity, which a [64,64] tanh net may underfit. Doubling layer width quadruples parameter count for that multi-modal value landscape without touching reward balance, exploration, lambda, or credit assignment (each already tuned or falsified). Expect higher success rate via far-tail conversion and lower mean distance; if capacity is not binding, watch for slower fitting within the fixed 30k-step budget. | 46 | 6.9 | 4.8 | reverted (worse) |
| 14 | 2026-08-25 | train.py: PPO_HYPERPARAMETERS adds use_sde=True (gSDE exploration; no other change) | Three coefficient tweaks then a capacity change all failed, so this switches exploration KIND: replace per-step i.i.d. Gaussian action noise with state-dependent (gSDE) noise whose samples are constant within a rollout, giving temporally smooth, directed exploration. Mechanism targets the observed binding constraint: median 3.7 cm hovering at the threshold suggests training-time action jitter blurs the final approach, and the deterministic eval policy inherits a jitter-corrupted mean. Expect near-miss conversion; risk is slower early fitting within the fixed 30k budget (same trap as exp 13). | 12 | 10.9 | 11.1 | reverted (worse) |

Diff summary (exp 13, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

Note (exp 13): hypothesis half-right, result worse — the bigger net DID fit value better (explained_variance hit 0.91-0.95 late, above every prior run) but rollout success was only 0.53 and still climbing steeply at the 30k cap (ep_len_mean falling 145 -> 119), i.e. classic under-convergence from slower per-sample learning. With timesteps frozen at 30k, network size acts as an optimization-speed knob, and exps 1/9 already showed optimization throughput is not what's binding — so capacity cannot pay off inside this fixed budget. Do not revisit wider nets unless the timestep budget changes; remaining untried levers are PROGRESS_COEFFICIENT scale-up (sparse/dense rebalancing risk) and ent_coef fine-tuning downward (0.005).

Diff summary (exp 14, filled after edit, before training): `robot_learning/train.py | 1 +` (adds `"use_sde": True` to PPO_HYPERPARAMETERS).

Note (exp 14): gSDE collapsed learning (rollout success plateaued ~0.20 vs ~0.5+ for every Gaussian run): noise sampled once per rollout acts as a persistent per-episode action bias on this torque-driven arm, which the policy must un-learn rather than average out, and std barely decayed (0.99 -> 0.72 over 30k) so the deterministic mean never sharpened. Lesson: i.i.d. Gaussian action noise is load-bearing here — it averages to zero within an episode and lets the mean controller converge. Do not switch exploration mechanisms again; safest remaining levers are ent_coef 0.005 fine-tune and PROGRESS_COEFFICIENT scale-up.

| 15 | 2026-08-25 | re-baseline at 60k budget: committed champion config re-run completely unchanged, only --timesteps 60000 (per budget change notice) | The budget doubling directly targets the falsified binding constraint: exps 9/13/14 were under-converged at the 30k cap, not bad ideas. Expect the unchanged champion config to convert near-misses with double the samples, scoring above its retired 55% and defining the new comparison baseline. | 55 | 6.5 | 3.0 | new best (baseline reset) |
| 16 | 2026-08-25 | train.py: POLICY_KWARGS net_arch [64, 64] -> [128, 128] (no other change; first width retest under the 60k budget) | Exp 13's width failure was purely under-convergence (rollout success still climbing steeply at the 30k cap, best-ever explained_variance 0.91-0.95) and its own note deferred a retest until the budget changed; exp 15 then proved the [64,64] champion genuinely plateaus (~0.71 rollout success by 28k) so extra samples alone cannot convert near-misses. The wider net's slower per-sample fitting should now have room to finish within 60k, converting near-threshold hoverers and behind-arm far-misses via better multi-modal value representation. Expect >55%; if rollout success again stalls well below the champion's ~0.71 by 60k, width is falsified independent of budget. | | | | |

Diff summary (exp 15, before training): intentionally EMPTY — no code edit by design (budget notice consequence #2 mandates the current committed configuration unchanged); verified `robot_learning/` clean in git status before training.

Diff summary (exp 16, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

Note (exp 15): hypothesis falsified — doubling the budget did NOT lift eval success: same 55% as the retired 30k score, rollout success plateaued ~0.71 by 28k steps and merely oscillated (0.63-0.78) through 60k despite healthy explained_variance (0.8-0.94), median stuck at exactly 3.0 cm. The asymptote is a real plateau, not under-convergence, so "needs longer training" is falsified beyond ~30k for this config. Next-session levers should shift to the remaining structural candidates: observation/joint-space target signal or target-radius curriculum (both need env un-freeze) — coefficient jitter on the current surface looks exhausted.
| 18 | 2026-08-25 | POLICY_KWARGS net_arch [64,64] -> [128,128] (retry at 60k budget) | Exp 13 showed the wider net still climbing at the 30k cutoff; the doubled budget removes the convergence constraint that killed it. Expect capacity to convert far-tail failures now. | 54 | 6.3 | 3.0 | reverted (worse) |
| 19 | 2026-08-25 | reach_env._observation(): append analytic-IK joint-space target signal - wrapped joint-angle deltas to both elbow-up/down solutions (+4 dims, obs 7 -> 11) | The eval plateau (median exactly 3.0 cm, rollout success ~0.71 asymptote) coincides with failures on behind-arm targets where the policy must infer both inverse-kinematics branches from a Cartesian error vector around the fully-extended reset singularity. Appending the wrapped joint-angle deltas to BOTH analytic IK solutions makes the required joint-space move explicit and turns branch selection into a linearly representable input rather than a learned multi-modal inference. Expect >55% via near-miss and behind-arm conversion; main risk is mild early-fitting slowdown from four extra input dimensions. | 100 | 2.5 | 2.5 | kept |
| 20 | 2026-08-26 | train.py: PPO_HYPERPARAMETERS gamma 0.99 -> 0.995 (no other change) | Scenario 2 moved success ~100 control steps into the future, but gamma=0.99 discounts a reward 100 steps out to 0.37x, so the value function propagates HOLD_COMPLETE_BONUS weakly through the hold phase and the policy undervalues simply staying inside the band. Raising gamma to 0.995 doubles the effective credit-assignment horizon (100 -> 200 steps) to match the new hold requirement while leaving the continuous-in-distance shaping untouched, which scenario 1 proved load-bearing. Expect more stable holds and a solid first scenario-2 baseline above trivial performance; main risk is slower early value fitting within the fixed 120k budget. | 0 | 2.1 | 2.1 | reverted (worse) |
| 21 | 2026-08-26 | reach_reward.py: DWELL_BONUS_PER_STEP 0.5 -> 5.0 (band occupancy pays meaningfully from the first held step; structure unchanged) | The escalating ramp pays only 0.005 on the first in-band step, so entering the band is short-term value-neutral versus parking just outside at ~2 cm (exp 20: 0% holds, mean dist 2.1 cm) while boundary-bouncing progress penalties make entry mildly negative. Raising DWELL_BONUS_PER_STEP to 5.0 gives every in-band step immediate dense credit (0.05 at entry ramping to 5.0), converting the all-or-nothing 100-step hold into accumulated occupancy reward that pulls the policy inside and keeps it there, directly targeting the precision+stopping constraint. Expect success well above the 0% baseline via band-entry conversion; main risk is larger return scale slowing value fitting within the fixed budget. | 0 | 2.1 | 2.1 | reverted (worse) |
| 22 | 2026-08-26 | reach_reward.py: add per-step proximity rent 0.2*exp(-distance/0.02) paid every step, continuous in distance (structure otherwise unchanged) | Both scenario-2 runs failed identically (0%, median 2.1 cm): the band payoff (ramped dwell + HOLD bonus 50) is invisible to the value function until a 100-step noise-free hold occurs, which exploration noise makes vanishingly rare, and the closeness potential differentiates entering vs parking outside by only ~0.7 total. A continuous per-step rent creates a dense standing gradient (value gap ~15 between parking at 0.5 cm vs 2 cm at gamma=0.99) that pulls the policy through the last centimeter and anchors it at the center, converting hovers into entries without touching thresholds or hold logic. Sized so the hover-at-center rent stream (~20) stays below the HOLD bonus (50) so completing remains strictly preferred. | 0 | 6.1 | 2.1 | reverted (worse) |
| 23 | 2026-08-26 | reach_reward.py: add second near-field exponential to _closeness_potential - NEAR_FIELD_COEFFICIENT 2.0 at NEAR_FIELD_LENGTH_SCALE 0.01 (success-threshold scale); far-field C=4 / lambda=0.05 unchanged | All three scenario-2 runs stalled identically (0%, median 2.1 cm) because outside the band a stationary policy earns ~0 regardless of distance: the only standing gradient is the lambda=0.05 potential tuned for the retired 3 cm touch task, while the escalating dwell pays almost nothing until an in-band entry occurs, which exploration noise never once produced in 120k steps (rollout success_rate stuck at 0). A second potential-based exponential with length scale equal to the new 1 cm threshold adds a smooth, kink-free standing value gradient (+~25/m at the observed 2.1 cm hover, roughly doubling local pull at the band edge, negligible beyond 5 cm) that pulls the policy through the last centimeter BEFORE entry, without touching far-field behavior or hold logic. Expect median final distance to drop below 1 cm so deterministic eval can complete holds; main risk is slower value fitting on the sharper near-field landscape within the fixed 120k budget. | 0 | 4.0 | 2.2 | reverted (worse) |
| 24 | 2026-08-26 | reach_env._observation(): append task-space end-effector velocity (analytic 2-link Jacobian applied to qvel, +2 dims, obs 12 -> 14) | All four scenario-2 failures share one signature: the approach stalls at ~2x the threshold and rollout success stays exactly 0, meaning stopping precision - not incentive scale - is binding. Holding within 1 cm requires nulling Cartesian velocity (task-space damping), but the policy only sees joint velocities, forcing it to co-learn the configuration-dependent Jacobian map with the control law itself, the same implicit-inference failure that exp 19's explicit IK deltas fixed for +45 points. Appending analytic EE velocity turns station-keeping into a linearly representable (error, error-rate) regulation problem; expect the deterministic hover point to move inside the band and holds to finally appear in rollouts. | 0 | 2.1 | 2.0 | reverted (worse) |
| 25 | 2026-08-26 | TEST: gamma 0.99 -> 0.995 via params | Pipeline validation of parameter mode. | - | - | - | error (proposal contains both params and a code edit — use one or the other) |
| 26 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 and log_std_init 0.0 -> -1.0 (exploration-noise scale reduction; both knobs serve the single noise-magnitude hypothesis, reward structure untouched) | All five scenario-2 failures share one signature (0% success, rollout success_rate exactly 0, eval median ~2.1 cm = 2x threshold): the policy never enters the 1 cm band even once during stochastic training, so dwell ramp and HOLD_COMPLETE_BONUS never deliver gradient and nothing selects for precision - every reward-side reweighting (exps 21-23) failed because it reweighted rewards the agent never samples, and explicit velocity feedback (exp 24) could not help because the value landscape near the band stays flat from lack of data. The upstream cause is exploration-noise magnitude: Gaussian torque noise starts at std 1.0 and only decayed to ~0.72 after 30k steps under ent_coef 0.01 (exp 14 log), which on this small torque-driven arm produces position jitter far larger than the 1 cm band, making multi-step in-band occupancy unreachable by chance; the curriculum ladder (3 cm x 5-step holds first) can only unlock if such streaks occur at all. Halving the entropy bonus pressure (ent_coef 0.005) lowers the std equilibrium for the whole run, and starting std at e^-1 ~= 0.37 instead of 1.0 makes band-entry streaks attainable from step 0, letting dense shaping still drive the approach while fine station-keeping gets sampled, the dwell/hold machinery finally receives training signal, and stage advancement becomes possible within the fixed 120k budget. Expect rollout success_rate to become nonzero and eval success above 0%; main risk is weaker early exploration slowing approach learning, which the dense progress+closeness shaping should absorb. | - | - | - | error (training failed:
Traceback (most recent call last):
  File "<frozen runpy>", lin) |
| 27 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 (halve entropy-bonus pressure; single-knob exploration-noise reduction after exp 26's log_std_init proved unusable at PPO top level) | All five scenario-2 failures share one signature (0% success, rollout success_rate exactly 0, eval median ~2.1 cm = 2x threshold): the policy never enters the 1 cm band even once during stochastic training, so dwell ramp and HOLD_COMPLETE_BONUS never deliver gradient and nothing selects for precision - reward-side reweightings (exps 21-23) failed because they reweighted rewards the agent never samples, and explicit velocity feedback (exp 24) could not help because the value landscape near the band stays flat from lack of data. Upstream cause: exploration-noise magnitude. Gaussian torque noise starts at std 1.0 and decayed only to ~0.72 after 30k steps under ent_coef 0.01 (exp 14 log), producing position jitter far larger than the 1 cm band on this small torque-driven arm, so multi-step in-band occupancy is unreachable by chance and the curriculum ladder (3 cm x 5-step holds first) cannot unlock. Halving ent_coef to 0.005 lowers the std equilibrium for the whole run so band-entry streaks become attainable while dense progress+closeness shaping still drives the approach; dwell/hold machinery then receives training signal and stage advancement becomes possible within the fixed 120k budget. Expect rollout success_rate to become nonzero and eval success above 0%; main risk is weaker exploration slowing approach learning. | - | - | - | error (training failed:
    ...<2 lines>...
        device=self.device,
        ^^^^^^^) |
