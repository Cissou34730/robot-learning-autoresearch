# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 2 measurements | Partially supported. The automatic baseline hypothesis was to establish the initial baseline, and it established substantial learned task performance near the objective. The measured task-reference result reaches 98.0% for the selected checkpoint, but the independent researcher panels remain at 97.0-97.5%, so the evidence does not support claiming that the human objective has been established. The baseline had no type-specific causal prediction; the observed training peak and endpoint regression are useful diagnostic signals but do not by themselves identify a mechanism. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Fine-tune the transferred checkpoint-100352 policy with the unchanged PPO task and policy architecture, reducing PPO learning_rate from 0.0003 to 0.0001 while retaining the other effective parameters. / parent working | ppo.learning_rate: 0.0003 -> 0.0001 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 3 measurements | Weakened. The exploratory question's useful-refinement branch was not observed: neither measured fine-tuned checkpoint improved the residual region or preserved broad task performance relative to the parent. This conclusion is limited to the transferred run, its learning-rate change, and the measured panels and seed. The unmeasured checkpoints remain unmeasured, and the evidence does not isolate learning rate as the causal source of regression. | working working; code revert |
| 3 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 2 measurements | Contradicted. The proposed fresh seed-1 run was expected to reproduce near-threshold performance near the experiment-1 selection point with similarly concentrated negative-angle failures. Neither measured replication checkpoint was near threshold, and the shared-panel geometry included extensive non-negative-angle failures. The alternative that the near-threshold result depends on seed-specific optimization or representation formation is supported in this tested seed contrast. This does not identify the causal source of the difference or estimate variance across the broader seed space, and unmeasured checkpoints remain unmeasured. | working working; code keep |
| 4 | Expand TRAINING_TARGET_RADIUS_RANGE from (0.14, 0.20) to (0.06, 0.20) meters; keep the official evaluation task, target-angle sampling, reward, observations, actions, and PPO hyperparameters unchanged. / parent working | Expand TRAINING_TARGET_RADIUS_RANGE from (0.14, 0.20) to (0.06, 0.20) meters; keep the official evaluation task, target-angle sampling, reward, observations, actions, and PPO hyperparameters unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 3 measurements | Partially supported. The exploratory question asked whether full-radius transfer could improve short-radius negative-angle failures while preserving broad competence. The protected panel shows a net improvement from 196/200 to 197/200 at the comparable checkpoint, with two prior short-radius failures resolved and all non-negative-angle successes retained. The result is incomplete because two short-radius failures remain, one long-radius negative-angle failure appears, and only one transferred seed and fixed development panels were measured. The researcher-panel improvement from 97.0% for the parent to 98.0% is not a clean causal comparison because its evaluation context changed. | working checkpoint-100352; best known checkpoint-100352; code keep |
