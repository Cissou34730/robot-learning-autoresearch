# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | This was a fresh baseline, whose recorded hypothesis was only to establish an initial reference for the human-defined objective; the proposal recorded no intervention-specific expected or contradicting observation. That baseline expectation was met: the policy learned substantially and supplied a comparable near-threshold reference. The evidence does not establish objective success, does not prove the late regression is causal, and does not justify selecting the endpoint over the earlier peak. The current investigation is resolved for lineage choice, while the scientific direction changes to improving geometric robustness. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Change only the researcher-owned training target sampler to draw 50% of targets uniformly from the observed -150 to -120 degree failure sector and 50% uniformly across the complete angular range; keep the fixed evaluation distribution unchanged. / parent working | Change only the researcher-owned training target sampler to draw 50% of targets uniformly from the observed -150 to -120 degree failure sector and 50% uniformly across the complete angular range; keep the fixed evaluation distribution unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was fewer negative-angle reach and hold failures while maintaining or improving reliability elsewhere. The recorded contradicting observation was similar focus-sector failures or a material decline outside the sector. Both contradicting outcomes occurred: focus-sector failures increased from 5 to 8 and 9, while new outside-sector failures appeared and grew from 3 to 9. The hypothesis is contradicted for this transferred run and recipe. The fixed-distribution evidence resolves the exposure-only investigation, although it cannot by itself distinguish a systematic limitation from PPO training variance. | working working; code revert |
| 3 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement | The replication was intended to test whether the baseline method's outcome was reproducible from fresh initialization. It is contradicted as an exact-performance expectation: the unchanged method produced a dramatically worse realization rather than a near-baseline policy. This does not show that the baseline recipe is intrinsically poor or that a mechanism intervention is unnecessary; it establishes substantial training variance and leaves the frequency of successful realizations unknown from one fresh replicate. The current investigation is sufficiently resolved for lineage choice and for the next direction: retain the strong baseline and test a mechanism-focused change by transfer, rather than interpret this failed realization as evidence for another sampler. | working working; code keep |
| 4 | Add a bounded per-step stability bonus proportional to the remaining margin inside the success tolerance, while leaving target sampling, task mechanics, and evaluation distribution unchanged. / parent working | Add a bounded per-step stability bonus proportional to the remaining margin inside the success tolerance, while leaving target sampling, task mechanics, and evaluation distribution unchanged.; files: robot_learning/scenario/reward.py, tests/scenario/test_reward.py | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements | The recorded expectation was fewer negative-angle failures and hold interruptions without increasing failures elsewhere. The hypothesis is contradicted for this transferred run and recipe: none of the measured checkpoints approached the baseline's 97.5% matched-panel result, hold interruptions were not lower than the baseline's two hold-limited failures, and no-reach failures increased substantially at the later checkpoints. The investigation is sufficiently resolved for the model and code decision. The early-versus-late variation shows that checkpoint selection and PPO dynamics matter, but it does not leave the reward mechanism decision arbitrary because every measured stability-reward checkpoint was below the retained baseline and the later failures broadened. | working working; code revert |
| 5 | Replace each wrapped inverse-kinematics angle residual in robot_learning/scenario/observations.py with its sine, preserving the 11-feature observation shape and leaving target sampling, task mechanics, reward, and evaluation distribution unchanged; add a focused regression test for the representation. / parent working | Replace each wrapped inverse-kinematics angle residual in robot_learning/scenario/observations.py with its sine, preserving the 11-feature observation shape and leaving target sampling, task mechanics, reward, and evaluation distribution unchanged; add a focused regression test for the representation.; files: robot_learning/scenario/observations.py, tests/scenario/test_observations.py | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement | The recorded expectation was fewer negative-angle no-reach episodes while retaining near-baseline hold interruptions and full-range success. The contradicting observation was that negative-angle failures remained or failures broadened outside the sector, with material hold degradation. The observed 66.5% success, broad outside-sector failures, and large increases in both no-reach and reached-but-not-held failures satisfy the contradicting condition and contradict the hypothesis for this transferred run and recipe. The investigation is resolved for the model and code decision; the result does not quantify seed-to-seed intervention frequency, but that uncertainty does not make the next direction arbitrary. | working working; code revert |
| 6 | Transfer the retained baseline and lower PPO learning_rate from 0.0003 to 0.0001, leaving the policy architecture, target sampler, reward, task mechanics, and evaluation distribution unchanged. / parent working | ppo.learning_rate: 0.0003 -> 0.0001 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was that the lower learning rate would reduce localized negative-angle no-reach failures while preserving full-range reach-and-hold reliability. The early checkpoint contradicts the improvement expectation because it retained the baseline's three no-reach failures and two hold-limited failures in the same sector. The comparable-horizon checkpoint additionally contradicts the preservation condition through lower success, more hold interruptions, and six outside-sector failures. The hypothesis is contradicted for this transferred run. The investigation is resolved for the tested optimization intervention and lineage decision; the early-versus-late contrast is useful evidence that this lower-rate recipe did not repair the gap, rather than evidence that another lower-rate checkpoint should be pursued. | working working; code revert |
| 7 | Increase the PPO MLP policy from [64, 64] to [128, 128] and transfer overlapping baseline policy parameters into the wider network while leaving the sampler, reward, observations, task mechanics, activation, and PPO optimization settings unchanged. / parent working | Increase the PPO MLP policy from [64, 64] to [128, 128] and transfer overlapping baseline policy parameters into the wider network while leaving the sampler, reward, observations, task mechanics, activation, and PPO optimization settings unchanged.; files: research/current_params.json, robot_learning/train.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was that the wider transfer would exceed the baseline's 97.5% matched-panel success by reducing focus-sector no-reach failures while preserving full-range reach-and-hold reliability. The recorded contradicting observation was unchanged focus failures, no improvement over baseline, or material outside-sector or hold-interruption failures. The early checkpoint contradicted the expectation on all principal dimensions: it scored below baseline, increased no-reach failures from 3 to 10, and added outside-sector failures. The comparable-horizon checkpoint further broadened failures and reduced success to 87.5%. The hypothesis is contradicted for this transferred run. The current investigation is sufficiently resolved for the tested capacity decision and lineage choice; the direction changes to a distinct behavior-preserving no-reach intervention. Stochastic realization remains an uncertainty, but the evidence does not make another width transfer or an immediate replication decision necessary. | working working; code revert |
| 8 | Change the researcher-owned training target sampler to draw radii uniformly from the full official 6-20 cm range while leaving target angles, reward, observations, task mechanics and PPO settings unchanged. / parent working | Change the researcher-owned training target sampler to draw radii uniformly from the full official 6-20 cm range while leaving target angles, reward, observations, task mechanics and PPO settings unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was that full-radius training would reduce failures below 14 cm and improve on the baseline's 97.5% matched-panel success without adding outside-sector failures or material hold degradation. The recorded contradicting observation was unchanged short-radius failures, added hold interruptions, outside-sector failures, or a material decline below baseline. The early checkpoint directly contradicted the expected reduction by reproducing the baseline's short-radius failure pattern. The comparable-horizon checkpoint also contradicted the expected success improvement: its reduced no-reach count was offset by more hold interruptions and the total remained five failures. The hypothesis is contradicted for this transferred run. The radial-coverage investigation is sufficiently resolved for the model and code decision, and the direction changes to a distinct geometry-specific or stochasticity-aware no-reach question; another radial measurement would not change that decision. | working working; code revert |
| 9 | Replace the redundant planar z residual in robot_learning/scenario/observations.py with a bounded inverse-kinematics branch-feasibility preference computed from the fixed joint limits, retaining the 11-feature observation shape and adding focused scenario coverage in tests/scenario/test_observations.py. / parent best_known | Replace the redundant planar z residual in robot_learning/scenario/observations.py with a bounded inverse-kinematics branch-feasibility preference computed from the fixed joint limits, retaining the 11-feature observation shape and adding focused scenario coverage in tests/scenario/test_observations.py.; files: robot_learning/scenario/observations.py, tests/scenario/test_observations.py | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement | The recorded expectation was that exposing bounded joint-limit branch feasibility would reduce negative-angle no-reach failures, exceed the baseline's 97.5% success, and preserve full-range reach-and-hold behavior. The contradicting observation was unchanged or increased local failures, failure to exceed baseline, or new outside-sector reach or hold failures. The candidate instead fell 5.5 percentage points below baseline, increased failures from 5 to 16, retained the diagnosed negative-angle no-reach pattern, and broadened failures outside the sector. The hypothesis is contradicted for this transferred run. The current investigation is sufficiently resolved for the model and code decision; the result does not prove that every geometry-specific feature is ineffective, but it makes another measurement of this candidate irrelevant to the next decision. | working working; code revert |
| 10 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements | The recorded expectation was near-baseline success at a comparable checkpoint with failures localized to the known negative-angle sector. The contradicting observation was a broad or materially degraded result, including failures outside that sector. The 100,352-step candidate was 51.5 percentage points below the baseline, and the endpoint remained 41.5 and 42.0 points below it on the two panels. The endpoint's consistent weakness across seeds rejects a panel-specific explanation. The hypothesis is contradicted for this fresh seed and recipe. The investigation is resolved for the model and lineage decision and changes the next direction to learning-process robustness; two weak fresh runs do not estimate the frequency of strong realizations. | working working; code restore |
| 11 | Increase PPO n_steps from 1024 to 2048 while transferring the working checkpoint-100352; keep the task, reward, observations, policy architecture, learning rate, and other training settings unchanged. / parent working | ppo.n_steps: 1024 -> 2048 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was near-baseline or better fixed-panel success at both an early checkpoint and the endpoint, with failures localized rather than broad. The endpoint supports that expectation: it reaches 98.0% on both panels, improves the matched seed-2000 result by 0.5 percentage points, matches seed 2200, and introduces no new outside-sector failures. The early candidate contradicts the expected stable early result through lower aggregate success and outside-sector hold failures. The hypothesis is partly supported for this transferred realization, not established as a causal reduction in PPO update noise because the early-versus-late contrast and declining training metrics leave checkpoint selection and stochastic dynamics unresolved. The investigation is resolved for lineage choice and remains useful with a concrete next action: test explicit checkpoint/process selection rather than repeat n_steps 2048 unchanged. | working checkpoint-120832; best known checkpoint-120832; code keep |
| 12 | Transfer the current working checkpoint-120832 with PPO entropy regularization reduced from 0.01 to 0.0; keep n_steps=2048, the policy, task, reward, observations, learning rate, and other settings unchanged. / parent working | ppo.ent_coef: 0.01 -> 0.0 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was an early candidate near the working endpoint and an endpoint that avoided the prior late decline, with at least 98.0% success and no outside-sector failures. The early candidate missed the success threshold by one point, and the endpoint showed renewed training drift, 92.5% fixed-panel success, and ten new outside-sector failures. The hypothesis is contradicted for this transferred realization. The tested investigation is resolved enough to change direction: zero entropy is not a safe stability intervention, while the remaining checkpoint-selection versus stochastic-dynamics distinction still has a concrete next action. | working working; code revert |
| 13 | Keep the restored PPO learning recipe unchanged while reducing checkpoint cadence from 5000 to 2048 training steps so comparable mid-run candidates and the endpoint can be selected and measured. / parent working | training.checkpoint_every_steps: 5000 -> 2048 | 6 measured checkpoints; 53 unmeasured checkpoints; research_evaluation/research_evaluation: 7 measurements | The recorded expectation was a dense-cadence mid-run checkpoint with near-98% matched-panel success and localized failures, followed by a later regression. The 69,632 checkpoint met the single-panel peak but failed the matched-panel stability test at 96.5%, and no neighboring measured interval matched the experiment 11 endpoint. This is the recorded contradicting observation: the endpoint is not uniquely robust, but neither is there a stable selectable interval. The hypothesis is contradicted for this realization. The current investigation is sufficiently resolved for lineage choice and changes direction from checkpoint selection to stochastic process robustness; the unresolved frequency of strong realizations is a next-experiment question, not a reason for another checkpoint scan. | working working; code revert |
| 14 | Reduce PPO optimization epochs per rollout from the current default to 5 while retaining the experiment 11 n_steps 2048 recipe and all task, observation and reward semantics. / parent working | ppo.n_epochs: None -> 5 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was that five optimization epochs would preserve near-98% comparable early and endpoint success, reduce the post-peak decline in training success and reward, and keep failures localized. The early candidate was already below experiment 11's matched endpoint, the endpoint did not recover the lost quality, and the training trajectory still declined from its mid-run peak to 0.95 success and substantially lower reward. Although failures did not broaden in the severe way seen in experiment 12, the intervention did not suppress drift or improve the localized gap. The hypothesis is contradicted for this transferred realization. The competing stochastic or checkpoint-specific explanation remains plausible, but the current investigation is sufficiently resolved for the model and recipe decision: another n_epochs setting is not justified by these results. | working working; code revert |
| 15 | Collect PPO rollouts with four parallel, independently seeded training environments while preserving the 2048-transition total rollout and all task, observation, and reward semantics. / parent working | training.n_envs: 1 -> 4 | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was that four independently seeded environments would preserve near-98% matched-panel endpoint reliability, reduce late training drift, and keep failures localized. The comparable early checkpoint did not establish an improvement, the endpoint was 6.0 and 6.5 percentage points below the retained policy, and the training trajectory still showed a substantial decline before an uninformative training-success recovery. The endpoint diagnostics also contradict the expected localized failure pattern. The hypothesis is contradicted for this transferred realization. The current investigation is resolved for lineage choice and for the rollout-diversity branch; it remains useful with a concrete next action because the negative-angle gap remains, but another rollout or update-count variant would not distinguish the remaining task-facing explanations. | working working; code revert |
| 16 | In robot_learning/scenario/reward.py, increase CLOSENESS_LENGTH_SCALE from 0.05 to 0.08 so the exponential closeness potential supplies more useful reward gradient at intermediate reach distances without changing hold semantics or PPO configuration. / parent working | In robot_learning/scenario/reward.py, increase CLOSENESS_LENGTH_SCALE from 0.05 to 0.08 so the exponential closeness potential supplies more useful reward gradient at intermediate reach distances without changing hold semantics or PPO configuration.; files: robot_learning/scenario/reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was reduced negative-angle no-reach failures with at least approximately 98% matched-panel success, no material hold-interruption increase, and no outside-sector failures. The no-outside-sector condition was met, but aggregate success did not improve, the negative-angle failures were unchanged at both measured checkpoints, and hold interruptions persisted. The hypothesis is contradicted for this transferred realization. The original alternative that the gap reflects a representation, control, or stochastic-policy limitation remains plausible, but the reward-credit explanation is resolved for lineage and direction choice. | working working; code revert |
| 17 | In robot_learning/scenario/observations.py, replace the three Cartesian end-effector-to-target residual features with target radius, sin(target angle), and cos(target angle), preserving the 11-feature observation shape and all hold/reward/PPO behavior. / parent working | In robot_learning/scenario/observations.py, replace the three Cartesian end-effector-to-target residual features with target radius, sin(target angle), and cos(target angle), preserving the 11-feature observation shape and all hold/reward/PPO behavior.; files: robot_learning/scenario/observations.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was at least 98% on both matched panels, removal of some established negative-angle no-reach failures, and no outside-sector or material hold degradation. The contradicting observation was below-98% performance with persistent negative-angle failures or broadened failures and hold interruptions. The measured candidates satisfy the contradicting condition at both checkpoints: the endpoint did not remove the incumbent failures, added negative-sector failures, and broadened failures outside the sector. The hypothesis is contradicted for this transferred realization, and the investigation is sufficiently resolved for the model and recipe decision rather than genuinely inconclusive. | working working; code restore |
| 18 | In robot_learning/scenario/policy_io.py, replace the identity physical-action map with a per-environment stateful exponential smoother using 0.75 current-command weight and 0.25 previous-command weight, clipping each policy command before smoothing and resetting the previous command at every episode boundary; keep the Cartesian observation and all task/hold/reward semantics unchanged. / parent working | In robot_learning/scenario/policy_io.py, replace the identity physical-action map with a per-environment stateful exponential smoother using 0.75 current-command weight and 0.25 previous-command weight, clipping each policy command before smoothing and resetting the previous command at every episode boundary; keep the Cartesian observation and all task/hold/reward semantics unchanged.; files: robot_learning/scenario/policy_io.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was that smoothing would remove some incumbent negative-angle reach or hold failures, approach or exceed 98.0% on both panels, and avoid material full-range or hold degradation. The hypothesis is contradicted: neither endpoint panel lost an incumbent failure identity, each gained one additional failed episode, and both endpoint scores fell below the working model. The early checkpoint likewise provided no repair, matching the working panel on seed 2200 but scoring lower on seed 2000. The investigation is sufficiently resolved for the model and recipe decision, not genuinely inconclusive; another measurement of the same smoother would not distinguish the remaining explanations or change the next action. | working working; code restore |
| 19 | In robot_learning/scenario/policy_io.py, preserve the existing Cartesian observation and add a per-environment target-conditioned Jacobian-transpose guidance term to each clipped policy command, with independent reset state at episode boundaries; add a focused researcher-owned mapping test in tests/scenario/test_policy_io.py. / parent working | In robot_learning/scenario/policy_io.py, preserve the existing Cartesian observation and add a per-environment target-conditioned Jacobian-transpose guidance term to each clipped policy command, with independent reset state at episode boundaries; add a focused researcher-owned mapping test in tests/scenario/test_policy_io.py.; files: robot_learning/scenario/policy_io.py, tests/scenario/test_evaluation.py, tests/scenario/test_policy_io.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was that the bias would remove at least one incumbent negative-angle failure, reach or exceed 98.0% on both panels, and avoid new outside-sector or hold failures. The panel-level threshold was met, but the intervention removed none of the incumbent failures at either checkpoint and introduced no behavioral change in the failure identities. The hypothesis is contradicted for this action-guidance mechanism: it did not repair the diagnosed reach-side gap, although it also did not broaden failures. The investigation is sufficiently resolved for the model and recipe decision, not genuinely inconclusive; another measurement of the same bias would not distinguish the remaining explanations or change the next action. | working working; code restore |
| 20 | Add a joint-velocity penalty to the researcher-owned reach reward, pass the MuJoCo joint velocities from the training environment, and preserve the existing observation representation, physical action mapping, and PPO runtime parameters. / parent working | Add a joint-velocity penalty to the researcher-owned reach reward, pass the MuJoCo joint velocities from the training environment, and preserve the existing observation representation, physical action mapping, and PPO runtime parameters.; files: robot_learning/scenario/environment.py, robot_learning/scenario/reward.py, tests/scenario/test_reward.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation was selective repair of the persistent failures through lower-velocity behavior while preserving at least 98.0% on both panels. Both measured checkpoints retained every incumbent failure identity, and both failed the 98.0% requirement on at least one panel; the peak also added six and four failures and the endpoint added four and four. The hypothesis is contradicted for this transferred realization. The current investigation is sufficiently resolved for the intervention and lineage decision, not genuinely inconclusive: the candidate is materially weaker and provides no repair, so another measurement of the same penalty would not change the decision. The physical-dynamics versus stochastic-realization distinction remains unresolved, but it does not make the next direction arbitrary because a distinct control intervention or a seed replication has a clear conditional purpose. | working working; code revert |
| 21 | In robot_learning/scenario/policy_io.py, scale every policy action by 0.8 before it reaches the fixed MuJoCo motor controls, and add researcher-owned coverage for the mapping. / parent working | In robot_learning/scenario/policy_io.py, scale every policy action by 0.8 before it reaches the fixed MuJoCo motor controls, and add researcher-owned coverage for the mapping.; files: robot_learning/scenario/policy_io.py, tests/scenario/test_policy_io.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | The recorded expectation is contradicted for this transferred realization. Static scaling removed none of the incumbent failures, both checkpoints missed the 98.0% requirement on at least one panel, and added failures reduced full-range reliability. The current investigation is sufficiently resolved for the intervention and lineage decision, not genuinely inconclusive: another measurement of the same 0.8-scaled policy would not change the rejection. The broader physical-dynamics versus stochastic-realization distinction remains unresolved, and its decision consequence is to run a distinct-seed matched transfer replication rather than another action-scaling value. | working working; code restore |
