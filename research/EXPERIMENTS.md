# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 4 measurements | This was a fresh baseline, whose recorded hypothesis was only to establish an initial reference for the human-defined objective; the proposal recorded no intervention-specific expected or contradicting observation. That baseline expectation was met: the policy learned substantially and supplied a comparable near-threshold reference. The evidence does not establish objective success, does not prove the late regression is causal, and does not justify selecting the endpoint over the earlier peak. The current investigation is resolved for lineage choice, while the scientific direction changes to improving geometric robustness. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Change only the researcher-owned training target sampler to draw 50% of targets uniformly from the observed -150 to -120 degree failure sector and 50% uniformly across the complete angular range; keep the fixed evaluation distribution unchanged. / parent working | Change only the researcher-owned training target sampler to draw 50% of targets uniformly from the observed -150 to -120 degree failure sector and 50% uniformly across the complete angular range; keep the fixed evaluation distribution unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements | The recorded expectation was fewer negative-angle reach and hold failures while maintaining or improving reliability elsewhere. The recorded contradicting observation was similar focus-sector failures or a material decline outside the sector. Both contradicting outcomes occurred: focus-sector failures increased from 5 to 8 and 9, while new outside-sector failures appeared and grew from 3 to 9. The hypothesis is contradicted for this transferred run and recipe. The fixed-distribution evidence resolves the exposure-only investigation, although it cannot by itself distinguish a systematic limitation from PPO training variance. | working working; code revert |
| 3 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement | The replication was intended to test whether the baseline method's outcome was reproducible from fresh initialization. It is contradicted as an exact-performance expectation: the unchanged method produced a dramatically worse realization rather than a near-baseline policy. This does not show that the baseline recipe is intrinsically poor or that a mechanism intervention is unnecessary; it establishes substantial training variance and leaves the frequency of successful realizations unknown from one fresh replicate. The current investigation is sufficiently resolved for lineage choice and for the next direction: retain the strong baseline and test a mechanism-focused change by transfer, rather than interpret this failed realization as evidence for another sampler. | working working; code keep |
