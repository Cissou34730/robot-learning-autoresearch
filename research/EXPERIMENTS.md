# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 2 measurements | The baseline expectation of establishing a competent initial policy was supported: learning was substantial and the best checkpoint approached the objective. The contradicting observation is that the completed 120,000-step run did not improve monotonically and both development results remained below the objective on at least one panel. Because this was a baseline with no intervention, the evidence does not attribute the outcome to a method change or establish reproducibility. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Expand the training target-radius distribution from 0.14–0.20 m to the full fixed-task range of 0.06–0.20 m; leave the protected task, evaluation panels, PPO method, and runtime parameters unchanged. / parent working | Expand the training target-radius distribution from 0.14–0.20 m to the full fixed-task range of 0.06–0.20 m; leave the protected task, evaluation panels, PPO method, and runtime parameters unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement; task_reference/task-reference-v1: 2 measurements | The hypothesis that full target-radius coverage would improve near-base reach-and-hold robustness while preserving baseline performance is contradicted on the measured panels: baseline performance was preserved, but not improved, and the negative-angle failure pattern persisted. This conclusion is limited to the transferred run and the paired 200-episode development measurements; it does not establish that the intervention never helps or determine the official benchmark result. | working working; code revert |
| 3 | Augment the researcher-owned observation with cosine and sine of the target's global angle while retaining the existing joint-state, relative-position, and IK-branch features; keep the PPO method and runtime parameters unchanged. / parent fresh | Augment the researcher-owned observation with cosine and sine of the target's global angle while retaining the existing joint-state, relative-position, and IK-branch features; keep the PPO method and runtime parameters unchanged.; files: robot_learning/scenario/observations.py, tests/scenario/test_environment.py | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement; task_reference/task-reference-v1: 1 measurement | The hypothesis that smooth global target-direction features would reduce the persistent negative-angle failures and improve fixed-panel success is contradicted. The candidate performed far worse, with failures across the full angular range and a large loss on both panels. This supports rejecting this specific observation augmentation and its fresh PPO result; it does not establish that all representation changes are harmful, nor does it identify whether the degradation came from representation interaction, optimization, or control behavior. | working working; best known working; code revert |
| 4 | Transfer from the working checkpoint with the PPO learning rate reduced from 0.0003 to 0.0001; keep the baseline observation, reward, target distribution, network, and other PPO parameters unchanged. / parent working | ppo.learning_rate: 0.0003 -> 0.0001 | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement; task_reference/task-reference-v1: 2 measurements | The hypothesis that a lower PPO learning rate would preserve high success, reduce late-training drift, and improve persistent negative-angle failures is contradicted on both development panels. The candidate declined materially below the incumbent and improved no discordant episode. This conclusion is limited to the tested 0.0001 learning rate, transferred initialization, PPO recipe, single training realization, and development panels; it does not reject every optimization intervention or establish the official benchmark result. | working working; code keep |
| 5 | Transfer from the working checkpoint with the baseline PPO configuration and increase the scenario action-cost coefficient from 0.01 to 0.05 to regularize large actuator commands while preserving the baseline observation, action mapping, target distribution, reward terms, and network. / parent working | Transfer from the working checkpoint with the baseline PPO configuration and increase the scenario action-cost coefficient from 0.01 to 0.05 to regularize large actuator commands while preserving the baseline observation, action mapping, target distribution, reward terms, and network.; files: research/current_params.json, robot_learning/scenario/reward.py | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement; task_reference/task-reference-v1: 1 measurement | The hypothesis that stronger action regularization would reduce excessive torque changes near difficult negative-angle targets while preserving incumbent success is contradicted. It neither preserved the incumbent's success nor improved the localized failure pattern: all incumbent research failures persisted and the task-reference candidate added 11 failures despite fixing one. This conclusion is limited to the tested coefficient 0.05, transferred initialization, PPO recipe, single training realization, and development panels; it does not establish that every control-aware reward or action strategy is ineffective, nor does it determine the official benchmark result. | working working; code revert |
| 6 | Transfer from working checkpoint-100352 using the baseline PPO recipe and apply one-step exponential actuator-command smoothing with factor 0.5 in policy I/O; reset the filter at every episode boundary. / parent working | ppo.learning_rate: 0.0001 -> 0.0003 | 1 measured checkpoint; 23 unmeasured checkpoints; research_evaluation/research_evaluation: 1 measurement; task_reference/task-reference-v1: 1 measurement | The hypothesis that one-step smoothing would reduce command reversals and improve difficult-angle and near-base reach-and-hold robustness without broadening failures is contradicted. The candidate preserved none of the incumbent's panel margin, fixed no incumbent failure, and added broad reach failures; this conclusion is limited to factor 0.5, the transferred PPO recipe, one training realization, and the development panels. It does not establish that every control-side mechanism is ineffective or determine the official benchmark result. | working working; code revert |
| 7 | Canonicalize the existing four inverse-kinematics joint-error features by ordering the two branch pairs from smallest to largest current joint-error norm; preserve the fixed robot, evaluation semantics, baseline reward, and baseline PPO runtime parameters. / parent fresh | Canonicalize the existing four inverse-kinematics joint-error features by ordering the two branch pairs from smallest to largest current joint-error norm; preserve the fixed robot, evaluation semantics, baseline reward, and baseline PPO runtime parameters.; files: robot_learning/scenario/observations.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 2 measurements | The hypothesis is partly supported. The expected observation was preserved or improved panel success with fewer negative-angle failures and no broad new failures. Success improved on both fixed panels and the original failure identities were removed, supporting the canonicalized-branch explanation. However, the candidate relocated failures to other angles and the research paired result had only eight discordant episodes, so the single fresh realization and fixed panels do not establish causal attribution or reproducibility. | working checkpoint-120832; best known checkpoint-120832; code keep |
