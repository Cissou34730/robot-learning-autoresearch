# Experiment log

**Best so far:** 0% (experiment 39)

## Lessons that shaped the current best (condensed from experiments 0-11)

- Exp 1 (n_steps 256, reverted): more updates alone did not help; noise-driven contact.
- Exp 2 (action cost 0.05): +8 pts. Flailing now pays; policy more directed.
- Exp 3 (SUCCESS_BONUS 25 -> 5): +26 pts. Sparse/dense scale balance stabilized value fitting — binding-constraint fix.
- Exp 4 (hard near-zone gate, reverted): discontinuity at the gate poisoned advantages. Lesson: shaping must be continuous in distance.
- Exp 5 (exponential closeness potential C=2, lambda=0.05): +4 pts, median 3.9 cm. Potential-based shaping works; healthiest training curve so far.
- Exp 6 (C=4): +4 pts, median 3.7 cm. Doubling C converted near-misses without instability.
- Exp 7 (C=8): 53%, reverted. Coefficient scaling exhausted around C=4.
- Exp 8 (ent_coef 0.01): +2 pts to 55%. Current champion; fresh-seed audit confirmed 54% on unseen targets.
- Exp 9 (n_epochs 20, reverted): faster start but mid-run overfit and exploration lock-down; optimization intensity is not the constraint.
- Exp 10 (ent_coef 0.02, reverted): collapsed hard; ent_coef peaked near 0.01.
- Exp 11 (lambda 0.08, reverted): collapsed to 9%. The sharp lambda=0.05 focus near target is load-bearing; do not stretch it outward.

## Remaining untried lever classes

gamma / gae_lambda (credit assignment), net_arch (capacity), PROGRESS_COEFFICIENT scale-up,
ent_coef fine-tune downward (0.005).

Full original notes for experiments 0-11: see `research/archive.md`.

Diff summary (exp 13, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

## Results table

| # | Date | Change | Hypothesis | Success % | Mean dist (cm) | Median dist (cm) | Verdict |
|---|------|--------|------------|-----------|----------------|------------------|---------|
| 13 | 2026-08-25 | train.py: POLICY_KWARGS net_arch [64, 64] -> [128, 128] (no other change) | Network capacity is the last untried lever class: the champion's value function plateaus at explained_variance 0.75-0.88 and deterministic eval fails precisely on behind-arm targets whose approach requires representing both IK branches around the fully-extended reset singularity, which a [64,64] tanh net may underfit. Doubling layer width quadruples parameter count for that multi-modal value landscape without touching reward balance, exploration, lambda, or credit assignment (each already tuned or falsified). Expect higher success rate via far-tail conversion and lower mean distance; if capacity is not binding, watch for slower fitting within the fixed 30k-step budget. | 46 | 6.9 | 4.8 | reverted (worse) |
| 14 | 2026-08-25 | train.py: PPO_HYPERPARAMETERS adds use_sde=True (gSDE exploration; no other change) | Three coefficient tweaks then a capacity change all failed, so this switches exploration KIND: replace per-step i.i.d. Gaussian action noise with state-dependent (gSDE) noise whose samples are constant within a rollout, giving temporally smooth, directed exploration. Mechanism targets the observed binding constraint: median 3.7 cm hovering at the threshold suggests training-time action jitter blurs the final approach, and the deterministic eval policy inherits a jitter-corrupted mean. Expect near-miss conversion; risk is slower early fitting within the fixed 30k budget (same trap as exp 13). | 12 | 10.9 | 11.1 | reverted (worse) |

Diff summary (exp 13, filled after edit, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

Note (exp 13): hypothesis half-right, result worse — the bigger net DID fit value better (explained_variance hit 0.91-0.95 late, above every prior run) but rollout success was only 0.53 and still climbing steeply at the 30k cap (ep_len_mean falling 145 -> 119), i.e. classic under-convergence from slower per-sample learning. With timesteps frozen at 30k, network size acts as an optimization-speed knob, and exps 1/9 already showed optimization throughput is not what's binding — so capacity cannot pay off inside this fixed budget. Do not revisit wider nets unless the timestep budget changes; remaining untried levers are PROGRESS_COEFFICIENT scale-up (sparse/dense rebalancing risk) and ent_coef fine-tuning downward (0.005).

Diff summary (exp 14, filled after edit, before training): `robot_learning/train.py | 1 +` (adds `"use_sde": True` to PPO_HYPERPARAMETERS).

Note (exp 14): gSDE collapsed learning (rollout success plateaued ~0.20 vs ~0.5+ for every Gaussian run): noise sampled once per rollout acts as a persistent per-episode action bias on this torque-driven arm, which the policy must un-learn rather than average out, and std barely decayed (0.99 -> 0.72 over 30k) so the deterministic mean never sharpened. Lesson: i.i.d. Gaussian action noise is load-bearing here — it averages to zero within an episode and lets the mean controller converge. Do not switch exploration mechanisms again; safest remaining levers are ent_coef 0.005 fine-tune and PROGRESS_COEFFICIENT scale-up.

| 15 | 2026-08-25 | re-baseline at 60k budget: committed champion config re-run completely unchanged, only --timesteps 60000 (per budget change notice) | The budget doubling directly targets the falsified binding constraint: exps 9/13/14 were under-converged at the 30k cap, not bad ideas. Expect the unchanged champion config to convert near-misses with double the samples, scoring above its retired 55% and defining the new comparison baseline. | 55 | 6.5 | 3.0 | new best (baseline reset) |
| 16 | 2026-08-25 | train.py: POLICY_KWARGS net_arch [64, 64] -> [128, 128] (no other change; first width retest under the 60k budget) | Exp 13's width failure was purely under-convergence (rollout success still climbing steeply at the 30k cap, best-ever explained_variance 0.91-0.95) and its own note deferred a retest until the budget changed; exp 15 then proved the [64,64] champion genuinely plateaus (~0.71 rollout success by 28k) so extra samples alone cannot convert near-misses. The wider net's slower per-sample fitting should now have room to finish within 60k, converting near-threshold hoverers and behind-arm far-misses via better multi-modal value representation. Expect >55%; if rollout success again stalls well below the champion's ~0.71 by 60k, width is falsified independent of budget. | | | | |

Diff summary (exp 15, before training): intentionally EMPTY — no code edit by design (budget notice consequence #2 mandates the current committed configuration unchanged); verified `robot_learning/` clean in git status before training.

Diff summary (exp 16, before training): `robot_learning/train.py | 2 +-` (`"net_arch": [64, 64]` -> `[128, 128]`).

Note (exp 15): hypothesis falsified — doubling the budget did NOT lift eval success: same 55% as the retired 30k score, rollout success plateaued ~0.71 by 28k steps and merely oscillated (0.63-0.78) through 60k despite healthy explained_variance (0.8-0.94), median stuck at exactly 3.0 cm. The asymptote is a real plateau, not under-convergence, so "needs longer training" is falsified beyond ~30k for this config. Next-session levers should shift to the remaining structural candidates: observation/joint-space target signal or target-radius curriculum (both need env un-freeze) — coefficient jitter on the current surface looks exhausted.
| 18 | 2026-08-25 | POLICY_KWARGS net_arch [64,64] -> [128,128] (retry at 60k budget) | Exp 13 showed the wider net still climbing at the 30k cutoff; the doubled budget removes the convergence constraint that killed it. Expect capacity to convert far-tail failures now. | 54 | 6.3 | 3.0 | reverted (worse) |
| 19 | 2026-08-25 | reach_env._observation(): append analytic-IK joint-space target signal - wrapped joint-angle deltas to both elbow-up/down solutions (+4 dims, obs 7 -> 11) | The eval plateau (median exactly 3.0 cm, rollout success ~0.71 asymptote) coincides with failures on behind-arm targets where the policy must infer both inverse-kinematics branches from a Cartesian error vector around the fully-extended reset singularity. Appending the wrapped joint-angle deltas to BOTH analytic IK solutions makes the required joint-space move explicit and turns branch selection into a linearly representable input rather than a learned multi-modal inference. Expect >55% via near-miss and behind-arm conversion; main risk is mild early-fitting slowdown from four extra input dimensions. | 100 | 2.5 | 2.5 | kept |
| 20 | 2026-08-26 | train.py: PPO_HYPERPARAMETERS gamma 0.99 -> 0.995 (no other change) | Scenario 2 moved success ~100 control steps into the future, but gamma=0.99 discounts a reward 100 steps out to 0.37x, so the value function propagates HOLD_COMPLETE_BONUS weakly through the hold phase and the policy undervalues simply staying inside the band. Raising gamma to 0.995 doubles the effective credit-assignment horizon (100 -> 200 steps) to match the new hold requirement while leaving the continuous-in-distance shaping untouched, which scenario 1 proved load-bearing. Expect more stable holds and a solid first scenario-2 baseline above trivial performance; main risk is slower early value fitting within the fixed 120k budget. | 0 | 2.1 | 2.1 | reverted (worse) |
| 21 | 2026-08-26 | reach_reward.py: DWELL_BONUS_PER_STEP 0.5 -> 5.0 (band occupancy pays meaningfully from the first held step; structure unchanged) | The escalating ramp pays only 0.005 on the first in-band step, so entering the band is short-term value-neutral versus parking just outside at ~2 cm (exp 20: 0% holds, mean dist 2.1 cm) while boundary-bouncing progress penalties make entry mildly negative. Raising DWELL_BONUS_PER_STEP to 5.0 gives every in-band step immediate dense credit (0.05 at entry ramping to 5.0), converting the all-or-nothing 100-step hold into accumulated occupancy reward that pulls the policy inside and keeps it there, directly targeting the precision+stopping constraint. Expect success well above the 0% baseline via band-entry conversion; main risk is larger return scale slowing value fitting within the fixed budget. | 0 | 2.1 | 2.1 | reverted (worse) |
| 22 | 2026-08-26 | reach_reward.py: add per-step proximity rent 0.2*exp(-distance/0.02) paid every step, continuous in distance (structure otherwise unchanged) | Both scenario-2 runs failed identically (0%, median 2.1 cm): the band payoff (ramped dwell + HOLD bonus 50) is invisible to the value function until a 100-step noise-free hold occurs, which exploration noise makes vanishingly rare, and the closeness potential differentiates entering vs parking outside by only ~0.7 total. A continuous per-step rent creates a dense standing gradient (value gap ~15 between parking at 0.5 cm vs 2 cm at gamma=0.99) that pulls the policy through the last centimeter and anchors it at the center, converting hovers into entries without touching thresholds or hold logic. Sized so the hover-at-center rent stream (~20) stays below the HOLD bonus (50) so completing remains strictly preferred. | 0 | 6.1 | 2.1 | reverted (worse) |
| 23 | 2026-08-26 | reach_reward.py: add second near-field exponential to _closeness_potential - NEAR_FIELD_COEFFICIENT 2.0 at NEAR_FIELD_LENGTH_SCALE 0.01 (success-threshold scale); far-field C=4 / lambda=0.05 unchanged | All three scenario-2 runs stalled identically (0%, median 2.1 cm) because outside the band a stationary policy earns ~0 regardless of distance: the only standing gradient is the lambda=0.05 potential tuned for the retired 3 cm touch task, while the escalating dwell pays almost nothing until an in-band entry occurs, which exploration noise never once produced in 120k steps (rollout success_rate stuck at 0). A second potential-based exponential with length scale equal to the new 1 cm threshold adds a smooth, kink-free standing value gradient (+~25/m at the observed 2.1 cm hover, roughly doubling local pull at the band edge, negligible beyond 5 cm) that pulls the policy through the last centimeter BEFORE entry, without touching far-field behavior or hold logic. Expect median final distance to drop below 1 cm so deterministic eval can complete holds; main risk is slower value fitting on the sharper near-field landscape within the fixed 120k budget. | 0 | 4.0 | 2.2 | reverted (worse) |
| 24 | 2026-08-26 | reach_env._observation(): append task-space end-effector velocity (analytic 2-link Jacobian applied to qvel, +2 dims, obs 12 -> 14) | All four scenario-2 failures share one signature: the approach stalls at ~2x the threshold and rollout success stays exactly 0, meaning stopping precision - not incentive scale - is binding. Holding within 1 cm requires nulling Cartesian velocity (task-space damping), but the policy only sees joint velocities, forcing it to co-learn the configuration-dependent Jacobian map with the control law itself, the same implicit-inference failure that exp 19's explicit IK deltas fixed for +45 points. Appending analytic EE velocity turns station-keeping into a linearly representable (error, error-rate) regulation problem; expect the deterministic hover point to move inside the band and holds to finally appear in rollouts. | 0 | 2.1 | 2.0 | reverted (worse) |
| 25 | 2026-08-26 | TEST: gamma 0.99 -> 0.995 via params | Pipeline validation of parameter mode. | - | - | - | error (proposal contains both params and a code edit — use one or the other) |
| 26 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 and log_std_init 0.0 -> -1.0 (exploration-noise scale reduction; both knobs serve the single noise-magnitude hypothesis, reward structure untouched) | All five scenario-2 failures share one signature (0% success, rollout success_rate exactly 0, eval median ~2.1 cm = 2x threshold): the policy never enters the 1 cm band even once during stochastic training, so dwell ramp and HOLD_COMPLETE_BONUS never deliver gradient and nothing selects for precision - every reward-side reweighting (exps 21-23) failed because it reweighted rewards the agent never samples, and explicit velocity feedback (exp 24) could not help because the value landscape near the band stays flat from lack of data. The upstream cause is exploration-noise magnitude: Gaussian torque noise starts at std 1.0 and only decayed to ~0.72 after 30k steps under ent_coef 0.01 (exp 14 log), which on this small torque-driven arm produces position jitter far larger than the 1 cm band, making multi-step in-band occupancy unreachable by chance; the curriculum ladder (3 cm x 5-step holds first) can only unlock if such streaks occur at all. Halving the entropy bonus pressure (ent_coef 0.005) lowers the std equilibrium for the whole run, and starting std at e^-1 ~= 0.37 instead of 1.0 makes band-entry streaks attainable from step 0, letting dense shaping still drive the approach while fine station-keeping gets sampled, the dwell/hold machinery finally receives training signal, and stage advancement becomes possible within the fixed 120k budget. Expect rollout success_rate to become nonzero and eval success above 0%; main risk is weaker early exploration slowing approach learning, which the dense progress+closeness shaping should absorb. | - | - | - | error (training failed:
Traceback (most recent call last):
  File "<frozen runpy>", lin) |
| 27 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 (halve entropy-bonus pressure; single-knob exploration-noise reduction after exp 26's log_std_init proved unusable at PPO top level) | All five scenario-2 failures share one signature (0% success, rollout success_rate exactly 0, eval median ~2.1 cm = 2x threshold): the policy never enters the 1 cm band even once during stochastic training, so dwell ramp and HOLD_COMPLETE_BONUS never deliver gradient and nothing selects for precision - reward-side reweightings (exps 21-23) failed because they reweighted rewards the agent never samples, and explicit velocity feedback (exp 24) could not help because the value landscape near the band stays flat from lack of data. Upstream cause: exploration-noise magnitude. Gaussian torque noise starts at std 1.0 and decayed only to ~0.72 after 30k steps under ent_coef 0.01 (exp 14 log), producing position jitter far larger than the 1 cm band on this small torque-driven arm, so multi-step in-band occupancy is unreachable by chance and the curriculum ladder (3 cm x 5-step holds first) cannot unlock. Halving ent_coef to 0.005 lowers the std equilibrium for the whole run so band-entry streaks become attainable while dense progress+closeness shaping still drives the approach; dwell/hold machinery then receives training signal and stage advancement becomes possible within the fixed 120k budget. Expect rollout success_rate to become nonzero and eval success above 0%; main risk is weaker exploration slowing approach learning. | - | - | - | error (training failed:
    ...<2 lines>...
        device=self.device,
        ^^^^^^^) |
| 28 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 (halve entropy-bonus pressure to lower the exploration-noise equilibrium; single-knob parameter-mode test, completes the interrupted exp 26/27 hypothesis on fixed infrastructure) | All five completed scenario-2 experiments share one signature (0% success, eval median ~2.1 cm = 2x threshold): the stochastic policy never once enters the 1 cm band long enough to sample the dwell ramp or HOLD_COMPLETE_BONUS, so every reward-side reweighting (exps 21-23) and observation addition (exp 24) had no experience to exploit - the binding constraint is upstream, in what the agent can physically sample. Upstream cause: exploration-noise magnitude. Exp 14's log showed Gaussian torque-noise std decaying only 0.99 -> 0.72 over 30k steps under ent_coef 0.01; at that scale end-effector jitter dwarfs the 1 cm band, making multi-step in-band occupancy unreachable by chance, the value function assigns holding zero predictive value, and the built-in curriculum ladder (3 cm x 0.1 s first stage) cannot unlock because even its entry streaks require more station-keeping than noise allows. Halving ent_coef to 0.005 halves the entropy bonus pressure that holds std up, lowering the noise equilibrium for the whole run so band-entry streaks become attainable while the dense progress+closeness shaping (proved load-bearing in scenario 1) keeps driving the approach; once entries occur, dwell ramp and hold bonus finally deliver gradient, curriculum stages can advance, and precision gets selected for. Expect nonzero rollout hold events (curriculum advancement becomes possible) and eval success above 0%; main risk is weaker exploration slowing approach learning early, which existing shaping should absorb. | 0 | 14.3 | 14.5 | reverted (worse) |
| 29 | 2026-08-26 | ppo: ent_coef 0.01 -> 0.005 AND reward: DWELL_BONUS_PER_STEP 0.5 -> 10.0 (two coordinated coefficient values serving one hypothesis: make in-band occupancy simultaneously samplable and valuable; structure untouched) | Scenario-2's five failures form a 2x2 whose fourth cell has never been tested. The chicken-and-egg loop: sampling hold rewards requires multi-step in-band occupancy; learning deliberate station-keeping requires hold-reward gradient. Exp 21 (high noise via ent_coef 0.01, DWELL 5.0) failed because stochastic torque jitter made 3 cm x 5-step streaks unreachable - rollout success_rate exactly 0 all run, so the amplified ramp never paid once (nothing to reinforce). Exp 28 (low noise via ent_coef 0.005, DWELL 0.5) proved the other half: lower noise made occupancy samplable for the first time (rollout success_rate 0.33-0.50 for ~20k steps) yet the run still collapsed to freeze-at-distance because each in-band step paid at most 0.5 while action cost drains ~0.03/step every step - occupancy was reachable but not worth keeping, so when noise decay ended incidental contact the action-cost economy made parking far away optimal (final mean 14.3 cm). Hypothesis: both knobs together break the loop - ent_coef 0.005 keeps band-entry streaks in the sampled data from step 0, and DWELL_BONUS_PER_STEP 10.0 turns every in-band step into the dominant local payoff (at stage 0: 2,4,6,8,10 per step vs <=0.1 action cost), so station-keeping is deliberately reinforced DURING the noise-decay window instead of relying on incidental contact, terminations become self-sustaining rather than accidental, and the built-in curriculum can advance through tighter thresholds and longer holds. Expect rollout success_rate to rise and stay nonzero past 20k steps (unlike exp 28), curriculum stage advancement beyond stage 0, and eval success above 0%; main risk is return-scale inflation slowing value fitting within the fixed 120k budget (watch explained_variance). | 0 | 4.3 | 3.7 | reverted (worse) |
| 30 | 2026-08-26 | reach_reward.py: replace the consecutive-streak dwell ramp (DWELL_BONUS_PER_STEP * held_steps / hold_steps_required, implicitly reset to zero whenever one step leaves the band) with a flat unconditional payment of DWELL_BONUS_PER_STEP on every in-band step; HOLD_COMPLETE_BONUS and all hold/threshold logic unchanged | The streak ramp makes partial competence nearly valueless: an isolated in-band step pays DWELL/100 (~0.005) while action cost drains up to ~0.1, so the oscillating policy that exploration noise actually produces earns almost nothing from occupancy - exps 28/29 showed rollout success_rate collapsing to exactly 0 tracking std decay (~60k steps) regardless of DWELL scale (0.5 vs 10.0), because occupancy only paid when a rare long noise-streak occurred, and by then PPO had already committed to the freeze basin behind a confidently-fit value function (explained_variance 0.97+). Paying DWELL_BONUS_PER_STEP on EVERY in-band step makes reward proportional to actual occupancy: a policy in-band half the time now earns half the max dwell stream instead of ~nothing, single noise-driven band entries immediately register as valuable outcomes for the value function from step 0, and hovering near/at the target becomes strictly more valuable than freezing far away, creating a continuous gradient along freeze -> hover -> hold. Expect rollout success_rate to stay nonzero through training instead of decaying to 0 with std (the signature failure of exps 28/29), curriculum stage advancement beyond stage 0 to become possible, and eval success above 0%; main risk is brief dip-in-and-out behavior being rewarded without true stability, which the untouched consecutive-step termination requirement still gates. | 0 | 6.2 | 4.7 | reverted (worse) |
| 31 | 2026-08-26 | reach_reward.py: add a per-step proximity rent PROXIMITY_RENT_COEFFICIENT * exp(-distance / PROXIMITY_RENT_LENGTH_SCALE) = 0.3 * exp(-distance / 0.02) paid on EVERY step as a function of current distance only; all existing terms (progress shaping, closeness potential, flat dwell, hold bonus, action cost) and all thresholds/hold logic unchanged | Exps 28-30 share one terminal signature: once exploration std decays, every existing reward term pays only on TRANSITIONS between states, so stationary income is ~0 at ANY distance while motion taxes torque - making park-far strictly optimal and sealing the freeze basin behind an explained_variance 0.94-0.998 value function (exp 28 ended at mean 14.3 cm). A standing per-step rent reverses exactly this economics: hovering near the target earns recurring income (0.11/step at 2 cm vs ~0.0003 at 14 cm) that dominates per-step action cost, so the value function learns 'being near target is intrinsically worth X/step' DURING the high-noise phase (the stream is attributable to region occupancy even when single-step causality is noise-masked), the mean controller stays anchored at the band edge instead of abandoning approach, and natural std decay then sharpens positioning INTO the band rather than into far-parking - converting entries from noise accidents into mean-policy behavior, which lets the dwell ramp and HOLD_COMPLETE_BONUS finally deliver gradient and the curriculum advance past stage 0. Sizing respects both failure lessons: coefficient 0.3 with length scale 2 cm keeps the discounted infinite stream at band center (0.3/(1-0.99) = 30) below HOLD_COMPLETE_BONUS (50) so completing a hold (+50, forfeiting only the remaining stream) remains strictly preferred over hovering forever, and the term is continuous in distance everywhere (exp 4 lesson). This is NOT a retest of the exhausted in-band occupancy family (exps 21/29/30 paid only inside 3 cm or 1 cm bands): the rent pays outside the band too, targeting the park-far basin itself; exp 22's weak rent (0.2, tested under the old high-noise regime where postmortem 28 notes verdicts may not transfer) lacked the gradient to overcome action-cost economics - 0.3 over a sharper 2 cm scale triples the pull at observed hover distances. Expect eval median distance to drop below 1 cm with nonzero curriculum advancement and success above 0%; main risks are return-scale inflation slowing value fitting within the fixed 120k budget (watch explained_variance) and dip-in-and-out rent farming, gated by the untouched consecutive-hold termination. | 0 | 11.9 | 10.3 | reverted (worse) |
| 32 | 2026-08-26 | reward: ACTION_COST_COEFFICIENT 0.05 -> 0.01 (5x motion-tax cut; no other change) | Exp 31 proved the macro-economy fix works (standing proximity rent eliminated park-far; terminal hover ~2 cm collecting income) but exposed the precise binding constraint: the parking equilibrium sits where marginal rent gain equals marginal station-keeping action cost - closing 2->1 cm earns +0.074/step while the micro-torques to hold tighter positions cost more than that at ACTION_COST_COEFFICIENT 0.05, so band ENTRY is economically neutral and never sampled late in training, leaving the dwell cliff (untouched flat DWELL_BONUS_PER_STEP 0.5 on every in-band step = ~6x the outside anchor income) invisible to an explained_variance-0.99 value function. The equilibrium is set by the local rent/cost RATIO, not by incentive levels (exp 31 note: do not crank the rent). Cutting the motion tax 5x shifts the stationary point inward past the 1 cm threshold, where the existing dwell cliff makes occupancy strictly optimal and self-reinforcing once finally sampled - giving the dwell/hold machinery its first-ever training signal and unlocking curriculum advancement past stage 0. Scenario-1's +8 pts from ACTION_COST 0.05 was earned against noise-driven flailing, a failure mode absent from every scenario-2 run (all stalled by under-approach or freeze, never by jitter), and exp 31's rent now anchors the policy near target even if torque becomes cheap. Expect the deterministic hover point to move inside the band and eval success above 0%, ideally nonzero rollout success persisting after std decay; main risk is looser station-keeping precision early, visible as reduced ep_len stability during the high-noise phase. | 0 | 21.5 | 23.0 | reverted (worse) |
| 33 | 2026-08-26 | reach_reward.py: add a standing per-step proximity rent PROXIMITY_RENT_COEFFICIENT * exp(-distance / PROXIMITY_RENT_LENGTH_SCALE) = 1.2 * exp(-distance / 0.01), paid on EVERY step as a function of current distance only; all existing terms and all thresholds/hold logic unchanged | The last centimeter before the success band is economically neutral, which is why band entry is never sampled after exploration noise decays. Evidence merge from two proven halves: exp 31 showed a standing per-step rent eliminates park-far and anchors the mean controller near the band (supported half), but its 2 cm length scale made the rent differential across the final centimeter (+0.072/step) smaller than the station-keeping action cost, so ENTRY was never profitable and the policy anchored at ~2x threshold; exp 32 then proved the base reward has no standing income at all, so no parking equilibrium exists without a rent - coefficient tuning alone cannot fix entry. This experiment makes the rent band-edge-FOCUSED: length scale 1 cm equal to the final-task threshold concentrates the gradient exactly where entry must happen (marginal pull 44/m at 1 cm vs 30/m under exp 31's shape), and coefficient 1.2 makes the across-edge differential (park at 2 cm vs 1 cm) 1.2*(e^-2-e^-1) ~= 0.28/step, ~3.9x exp 31's insufficient 0.072, so closing the last centimeter strictly dominates station-keeping cost while far-field contamination stays negligible (at 3 cm the rent is 0.06/step vs ~2.2 from existing closeness shaping, so approach dynamics are untouched). Accounting hazard from exp 31 is re-derived correctly: in-band occupancy auto-terminates within hold_steps_required steps because _held_steps increments unconditionally in-band, so no agent can hover in-band collecting rent+dwell forever; the bounded comparison is completion value (dwell*N + HOLD_COMPLETE_BONUS >= 50.5 at every curriculum stage) versus perpetual outside-parking at the edge (~44 discounted-perpetuity upper bound), so completion remains strictly preferred with margin. Single hypothesis: entry fails because it is economically neutral; a band-edge-focused standing rent makes entry both reachable-during-noise (standing income keeps the mean controller at the band edge while std is high, generating sampled crossings for attribution) and profitable-once-found (steep local gradient hands off to the dwell cliff before PPO commitment seals the basin). Expect rollout success_rate to persist nonzero past std decay (the signature failure of exps 28-32), terminal deterministic hover distance INSIDE or within ~1.2 cm of target (vs exp 31's 2 cm anchor), curriculum advancement beyond stage 0, and eval success above 0%. Main risks: sharper near-field landscape slowing value fitting (watch explained_variance), and if entry economics are still marginal the failure signature will be a new anchor between 1 and 2 cm. | 0 | 12.5 | 11.8 | reverted (worse) |
| 34 | 2026-08-26 | reach_reward.py: subtract an unconditional per-step time penalty TIME_PENALTY_PER_STEP = 0.05 on EVERY step regardless of position or band membership; all existing terms (progress shaping, closeness potential, flat dwell, hold bonus, action cost) and all thresholds/hold logic unchanged | Every scenario-2 failure shares one economic signature: non-termination is FREE. Stationary income is ~0 at any distance (exp 32 proved there are no equilibria on the base reward), so freeze-far and hover basins are comfortably fit by the value function (explained_variance 0.94-0.998) while the only behaviors that ever escape them - early terminations during the high-noise phase (rollout success_rate 0.33-0.71 in every run) - carry weak relative advantage: a terminating rollout differs from a non-terminating one by roughly one HOLD_COMPLETE_BONUS diluted across a long horizon plus small dwell, insufficient to outweigh attribution masking before std decays and entries vanish (the lockstep success-to-zero collapse of exps 28-33). An unconditional time penalty reverses the comfort: every step spent NOT completing costs 0.05 (max exposure 25 per 500-step episode vs HOLD_COMPLETE_BONUS 50, so completion stays strictly dominant), which (a) widens the advantage gap of naturally occurring early terminations DURING the high-noise phase when they are frequent - a rollout terminating at ~370 steps now beats an identical non-terminating one by ~6.5 reward units on top of existing terms, giving the approach-and-enter decision its strongest-ever per-sample gradient precisely when entry samples exist; (b) makes every parking basin strictly losing - their value visibly decays with horizon instead of settling, so the late-phase confident fit seals a worse basin each iteration rather than a comfortable one; (c) pressures the curriculum gate (0.6 over 15 episodes) to be crossed while noise-driven touches are still plentiful - stage 0 requires only a single touch within 3 cm, and peaks of 0.455-0.714 show the gate was always within reach but never sustained. Unlike the exhausted occupancy/rent families (which pay for WHERE the agent is), this acts on WHEN the episode ends; it adds no positive standing income, creates no hover trap, and respects exp 31's accounting hazard with wide margin (penalty stream reduces, never inflates, non-completing returns). Expect rollout success_rate to stay nonzero longer than the std-tracking collapses of exps 28-33 and/or first-ever curriculum advancement beyond stage 0, and eval success above 0%; main risks: (i) if terminations go extinct late as before, the penalty becomes inert constant drift rather than a fix - the prediction to watch is WHEN success_rate decays relative to prior runs, not just the final score; (ii) flailing-for-truncation is unprofitable because truncation bootstraps rather than terminates and precision cannot be flailed into; (iii) return-scale shift of -25 slows value fitting slightly within the fixed 120k budget. | 0 | 9.8 | 10.2 | reverted (worse) |
| 35 | 2026-08-26 | reach_reward.py: multiply the existing progress term by a distance-scheduled approach gain - PROGRESS_COEFFICIENT * (1 + APPROACH_GAIN_COEFFICIENT * exp(-current_distance / APPROACH_GAIN_LENGTH_SCALE)) * (previous_distance - current_distance) with APPROACH_GAIN_COEFFICIENT 20.0 and APPROACH_GAIN_LENGTH_SCALE 0.015 m; closeness potential, flat dwell, hold bonus, action cost, and all thresholds/hold logic unchanged | Every scenario-2 failure shows one geometry: the mean policy asymptotes just outside the active band (~2-3 cm) with touches generated purely by exploration noise; rollout success_rate slides below the 0.6-over-15-episodes advancement gate before reliable approach is learned (exp 34 log: 0.667 peak by iteration 2, monotonic slide from t=2k while std is still ~1.0), then noise death locks it out. Under the base economy the marginal payoff of closing the final centimeters is economically marginal against station-keeping cost (~0.064/mm at 2 cm from progress+closeness slopes vs accumulated micro-torque cost), so the mean never crosses the edge and every entry stays a noise accident. All eight prior economies reweighted LEVELS (rents/dwell pay for standing still - creating or shifting parking equilibria) or added constants (time penalty - advantage-inert); none changed the relative profitability of MOVING inward versus outward near the band. A distance-scheduled progress gain does exactly that: closing 1 mm at 2 cm now pays ~6.3x more (approach actions get strongly reinforced from ORDINARY approach data, not rare terminal events), retreating 1 mm near the band costs ~6.3x more (noise-driven in/out random walks acquire inward drift precisely during the high-noise window when the curriculum gate must be crossed), while paying exactly zero when stationary (no standing income -> no new parking basin, unlike rent exps 22/31/33) and <=1.7x beyond 5 cm / ~1.0x beyond 10 cm (far-field approach proven in scenario 1 is untouched). The multiplier is smooth and continuous everywhere (exp 4 lesson). Expect the first-ever curriculum advancement past stage 0 (sustained >=0.6 success over a 15-episode window during the noisy phase), a deterministic hover point at or inside the active band, and eval success above 0%; main risks are inflated return variance early from amplified negative progress during flailing near the target (watch value_loss/explained_variance) and the same std-tracking collapse if transition-reweighting alone cannot beat attribution masking - which, if observed, completes the sweep of every reward-term family (level, occupancy, flat-transition, gated-transition, time) and makes the class-6 (learning algorithm) case decisive. | 0 | 23.1 | 25.1 | reverted (worse) |
| 36 | 2026-08-26 | Unchanged control using the transfer checkpoint and current curriculum | Establish the fixed-evaluator baseline for the current curriculum. | 0 | 2.2 | 2.1 | kept (baseline) |
| 37 | 2026-08-26 | Switch training and evaluation from PPO to SAC with replay and automatic entropy tuning | Off-policy replay will preserve rare in-band trajectories after fresh exploration declines, while SAC entropy tuning will maintain useful exploration and improve deterministic hold success. | - | - | - | error (pytest failed:

=================================== ERRORS =====================) |
| 38 | 2026-08-26 | Increase PPO gamma from 0.99 to 0.995. | A longer return horizon will assign more value to reaching the target and sustaining the final 100-step hold, improving deterministic evaluation success. | 0 | 2.4 | 2.3 | reverted (equal) |
| 39 | 2026-08-27 | Unchanged control using the transfer checkpoint and current curriculum | Establish the fixed-evaluator baseline for the current curriculum. | 0 | 2.1 | 2.0 | kept (baseline) |
| 40 | 2026-08-27 | reach_env.py: append normalized current hold-streak progress (held_steps / hold_steps_required, clipped to 1.0) to the observation (+1 dimension) | Explicit hold-streak progress will let PPO distinguish near-target states that are successfully stabilizing from near-target states that are repeatedly leaving the band, improving credit assignment for station-keeping and converting the current 0% deterministic hold policy without changing rewards or task invariants. | - | - | - | error (training failed:
  File "C:\Users\cyril.beurier\code\robot-learning-autoresearch) |
| 41 | 2026-08-27 | Increase PPO n_steps from 1024 to 2048. | Longer on-policy rollouts will reduce update noise and retain more rare target-entry trajectories per policy update, improving credit assignment for the existing reach-and-hold signal without changing the task or reward. | 0 | 2.4 | 2.3 | reverted (equal) |
| 42 | 2026-08-27 | policy activation: tanh -> relu | A ReLU policy will represent the sharp near-target and hold-value transition with fewer saturation effects than tanh, improving deterministic reach-and-hold success within the fixed 120000-transition budget. | 0 | 3.9 | 2.1 | reverted (equal) |
| 43 | 2026-08-27 | reach_env.py: append normalized current hold-streak progress to the observation (+1 dimension) | Providing the current consecutive in-band hold progress will let PPO distinguish stable near-target states from transient contacts, improving credit assignment for station-keeping and producing deterministic reach-and-hold success within the fixed 120000-transition budget. | - | - | - | error (pytest failed:
FF....................                                           ) |
| 44 | 2026-08-27 | Switch training and evaluation from PPO to fresh SAC with replay and automatic entropy tuning. | Off-policy replay will retain rare target-entry trajectories after fresh entries decline, while SAC automatic entropy tuning maintains useful exploration long enough to learn a deterministic reach-and-hold policy within the fixed 120000-transition budget. | - | - | - | error (edits touch files outside the allowed research surface: ['robot_learning/evaluat) |
| 45 | 2026-08-27 | Increase PPO gae_lambda from 0.95 to 0.99. | A higher GAE lambda will propagate the delayed reach-and-hold return through the 100-step hold phase with less bias, improving PPO credit assignment for deterministic station-keeping within the fixed 120000-transition budget. | 0 | 2.1 | 2.0 | reverted (equal) |
| 46 | 2026-08-27 | ppo: learning_rate 0.0003 -> 0.0001 | A lower PPO learning rate will reduce premature policy and critic commitment while exploration noise decays, allowing rare target-entry and hold trajectories to shape a stable deterministic reach-and-hold policy within the fixed 120000-transition budget. This changes only update magnitude; reward, observations, curriculum, evaluator, seed, and transition budget remain fixed. | 0 | 2.1 | 2.0 | reverted (equal) |
| 47 | 2026-08-27 | ppo: batch_size 64 -> 128 | A larger PPO minibatch will reduce noisy policy and value updates from rare target-entry trajectories, making the learned value landscape less sensitive to individual exploration-driven contacts and improving deterministic reach-and-hold success within the fixed 120000-transition budget. | 0 | 2.1 | 2.0 | reverted (equal) |
| 48 | 2026-08-27 | ppo: clip_range 0.2 -> 0.1 | A smaller PPO clip range will reduce premature policy commitment caused by rare exploration-driven contacts, allowing target-entry and hold trajectories to influence the deterministic policy longer within the fixed 120000-transition budget without changing the benchmark, reward, observations, curriculum, seed, or evaluator. | 0 | 2.1 | 2.0 | reverted (equal) |
| 49 | 2026-08-27 | ppo: n_steps 1024 -> 4096 | A 4096-transition PPO rollout will preserve more rare target-entry and near-hold trajectories in each update, reducing update-to-update noise and improving credit assignment for the delayed reach-and-hold signal within the fixed 120000-transition budget. | 0 | 3.2 | 3.2 | reverted (equal) |
| 50 | 2026-08-27 | ppo: vf_coef 0.5 -> 0.1 | Reducing PPO's value-loss coefficient will prevent the critic from overfitting and sealing the entry-free basin, allowing rare target-entry advantages to shape the deterministic reach-and-hold policy within the fixed 120000-transition budget. | 0 | 2.1 | 2.0 | reverted (equal) |
| 51 | 2026-08-27 | ppo: n_epochs 10 -> 5 | Reducing PPO update passes per rollout will reduce premature critic and policy commitment to the entry-free basin, allowing rare target-entry trajectories to influence the deterministic reach-and-hold policy longer within the fixed 120000-transition budget without changing the benchmark, reward, observations, seed, or transfer checkpoint. | 0 | 2.1 | 2.0 | reverted (equal) |
| 52 | 2026-08-27 | ppo: normalize_advantage true -> false | Disabling PPO advantage normalization will preserve the absolute magnitude of rare target-entry and hold advantages instead of diluting them against the overwhelmingly common failure trajectories, allowing those trajectories to shape the deterministic reach-and-hold policy within the fixed 120000-transition budget without changing the reward, observations, curriculum, seed, or evaluator. | 0 | 2.1 | 2.0 | reverted (equal) |
| 53 | 2026-08-27 | ppo: add target_kl 0.03 to constrain large policy updates | Constraining PPO updates when the approximate KL divergence exceeds 0.03 will prevent rare exploration-driven target-entry trajectories from causing premature policy commitment, preserving useful approach and hold behavior long enough to produce deterministic reach-and-hold success within the fixed 120000-transition budget. | 0 | 2.1 | 2.1 | reverted (equal) |
| 54 | 2026-08-27 | ppo policy initialization: ortho_init true -> false | Disabling orthogonal initialization for the PPO policy will reduce the overly confident early action/value representation inherited during resumed training, preserving useful exploration and allowing rare target-entry trajectories to shape a deterministic reach-and-hold policy within the fixed 120000-transition budget without changing the benchmark, reward, observations, seed, or transfer checkpoint. | 0 | 2.1 | 2.0 | reverted (equal) |
| 55 | 2026-08-27 | ppo: max_grad_norm 0.5 -> 0.1 | A tighter PPO gradient-norm cap will reduce rare-transition update spikes and prevent premature commitment to the entry-free basin, allowing target-entry and hold advantages to shape the deterministic reach-and-hold policy within the fixed 120000-transition budget. | 0 | 2.1 | 2.0 | reverted (equal) |
| 56 | 2026-08-27 | train.py: enable PPO gSDE with sde_sample_freq=1, resampling state-dependent exploration every control step | The prior gSDE attempt failed because noise persisted across an entire rollout and acted as a per-episode torque bias; resampling gSDE every control step should retain state-dependent exploration while removing that persistent bias, allowing the deterministic PPO mean to learn reach-and-hold behavior within the fixed 120000-transition budget. | 0 | 3.4 | 2.4 | reverted (equal) |
| 57 | 2026-08-27 | policy: net_arch [64, 64] -> [32, 32] | A smaller policy and value network will fit the low-dimensional 11-feature reach-and-hold task faster per sample, allowing PPO to retain useful target-entry and station-keeping behavior within the fixed 120000-transition budget instead of spending the budget fitting unnecessary capacity. | 0 | 5.3 | 2.2 | reverted (equal) |
| 58 | 2026-08-27 | ppo: gae_lambda 0.95 -> 1.0 | Using the unbiased full-return GAE setting will propagate rare target-entry and reach-and-hold credit through the 100-step hold phase more completely than gae_lambda 0.95, allowing those trajectories to shape the deterministic policy within the fixed 120000-transition budget without changing the benchmark, reward, observations, seed, or transfer checkpoint. | 0 | 2.2 | 2.0 | reverted (equal) |
| 59 | 2026-08-27 | policy: set log_std_init from 0.0 to -1.0 for fresh PPO initialization | Starting PPO with lower action-log-standard-deviation will make fine station-keeping and target-band occupancy samplable from the beginning, allowing the deterministic policy to learn reach-and-hold behavior within the fixed 120000-transition budget instead of collapsing as exploration noise decays. | 0 | 6.7 | 2.0 | reverted (equal) |
| 60 | 2026-08-27 | env: curriculum_stage_advance_success_rate 0.6 -> 0.3 | Lowering the curriculum advancement gate will let the repeated early stage-0 target contacts advance the policy to the 1 cm and longer-hold stages before PPO exploration decays, exposing useful reach-and-hold trajectories while leaving the fixed task thresholds, hold requirement, evaluator, seed, and transition budget unchanged. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 61 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | The current experiment cannot collect any transitions because Windows SubprocVecEnv exits during startup with EOFError. Using the existing single-process DummyVecEnv path will allow the unchanged transfer PPO training run to complete within the fixed 120000-transition budget, so the fixed evaluator can measure whether the current configuration has reach-and-hold capability. | 0 | 2.1 | 2.1 | reverted (equal) |
| 62 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after 5 completed episodes instead of 15 will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at the entry stage. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, evaluator, seed, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 63 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Windows SubprocVecEnv exits during startup with EOFError, preventing parameter experiments from training. Making the existing single-process DummyVecEnv path the default will reliably complete the fixed 120000-transition transfer PPO run and permit a fixed-evaluator measurement. | 0 | 2.1 | 2.1 | reverted (equal) |
| 64 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after 5 completed episodes instead of 15 will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at stage 0. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, seed, evaluator, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 65 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after 5 completed episodes instead of 15 will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at stage 0. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, seed, evaluator, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 66 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after five completed episodes instead of fifteen will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at stage 0. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, seed, evaluator, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 67 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Making the existing single-process DummyVecEnv path the default will let the runner complete the fixed 120000-transition transfer PPO run and fixed 200-episode evaluation on Windows instead of failing during SubprocVecEnv startup. | 0 | 2.1 | 2.1 | reverted (equal) |
| 68 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after five completed episodes instead of fifteen will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at stage 0. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, evaluator, seed, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 69 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Advancing the curriculum after five completed episodes instead of fifteen will expose the policy to the 1 cm and longer-hold stages while exploration is still useful, allowing PPO to learn the fixed reach-and-hold task rather than remaining at stage 0. This changes only the curriculum observation-window length; thresholds, hold requirement, targets, seed, evaluator, and transition budget remain fixed. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 70 | 2026-08-27 | env: curriculum_stage_advance_min_episodes 15 -> 5 | Reducing the stage-advancement evidence window will let the curriculum promote transient but repeatable early reach-and-hold competence before PPO exploration decays, exposing progressively harder stages within the fixed budget. | - | - | - | error (training failed:
    venv = make_vec_env(
        ENVIRONMENTS[args.env],
    ..) |
| 71 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Making the existing single-process DummyVecEnv path the default will let the runner complete the fixed 120000-transition transfer PPO run and fixed 200-episode evaluation on Windows instead of failing during SubprocVecEnv startup. | 0 | 2.1 | 2.1 | reverted (equal) |
| 72 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Making the existing single-process vector-environment path the default will let the fixed 120000-transition transfer PPO run and fixed 200-episode evaluation complete reliably on Windows, enabling a valid measurement of the current reach-and-hold configuration without changing the benchmark or learning method. | 0 | 2.1 | 2.1 | reverted (equal) |
| 73 | 2026-08-27 | reach_env.py: append normalized signed distance change since the previous control step to the observation (+1 dimension, 11 -> 12) | An explicit normalized distance-rate signal will let PPO distinguish inward approach from outward retreat and provide the damping cue needed for station-keeping, improving deterministic reach-and-hold success within the fixed 120000-transition budget without changing rewards, task invariants, curriculum, or evaluator. | 0 | 4.4 | 2.0 | reverted (equal) |
| 74 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Making the existing single-process vector-environment path the default will let the fixed 120000-transition transfer PPO run and fixed 200-episode evaluation complete reliably on Windows, enabling a valid measurement of the current reach-and-hold configuration without changing the benchmark, reward, observations, curriculum, seed, transition budget, or evaluator. | 0 | 2.1 | 2.1 | reverted (equal) |
| 75 | 2026-08-27 | train.py: default --n-envs 4 -> 1, using DummyVecEnv instead of Windows SubprocVecEnv | Making the existing single-process vector-environment path the default will allow the fixed 120000-transition transfer PPO run and fixed 200-episode evaluation to complete reliably on Windows, producing a valid measurement without changing the benchmark, reward, observations, curriculum, seed, transition budget, or evaluator. | 0 | 2.1 | 2.1 | reverted (equal) |
