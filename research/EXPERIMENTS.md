# Experiment log

| # | Operation / parent | Intervention | Checkpoint / panel results | Hypothesis assessment | Final decision |
|---:|---|---|---|---|---|
| 1 | Fresh baseline / parent fresh | Fresh baseline | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 3 measurements | Supported for the baseline's intended purpose of establishing a strong initial reference: the learned policy substantially improved measured task success and reached 98% on one independent development panel. This does not establish the official objective, and the endpoint's research-panel result does not overturn the protected-panel regression because the paired research outcomes were identical and the panels are distinct. The assessment is limited to this single training run and the measured checkpoints. | working checkpoint-100352; best known checkpoint-100352; code keep |
| 2 | Change TRAINING_TARGET_RADIUS_RANGE from the far-target-only interval (0.14, 0.20) to the official full interval (0.06, 0.20), preserving the existing robot, observation, action, reward, hold, and evaluation semantics. / parent best_known | Change TRAINING_TARGET_RADIUS_RANGE from the far-target-only interval (0.14, 0.20) to the official full interval (0.06, 0.20), preserving the existing robot, observation, action, reward, hold, and evaluation semantics.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 3 measured checkpoints; 21 unmeasured checkpoints; research_evaluation/research_evaluation: 3 measurements; task_reference/task-reference-v1: 3 measurements | Partially supported. Full-radius exposure removed one of the selected baseline's small-radius failures at checkpoint-100352 and preserved 98% task-reference success, while the endpoint recovered one percentage point relative to the experiment-1 endpoint. However, the selected candidate gained an outer-radius failure with no net success gain, the repeated negative-angle failure at episode 102 persisted, and researcher-evaluation outcomes were unchanged across measured checkpoints. The single transferred run and one fixed development panel do not establish that radius coverage caused the observed redistribution or that the intervention improves the official task distribution. | working checkpoint-100352; code keep |
| 3 | Change the researcher-owned training target sampler to use the full 6-20 cm radius range but draw 50% of training targets from the observed hard-angle sector -170 to -90 degrees, with the remaining 50% sampled uniformly over the full angle range. Evaluation, robot mechanics, observation semantics, action mapping, reward, and hold criteria remain unchanged. / parent working | Change the researcher-owned training target sampler to use the full 6-20 cm radius range but draw 50% of training targets from the observed hard-angle sector -170 to -90 degrees, with the remaining 50% sampled uniformly over the full angle range. Evaluation, robot mechanics, observation semantics, action mapping, reward, and hold criteria remain unchanged.; files: robot_learning/scenario/environment.py, tests/scenario/test_environment.py | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 3 measurements | Contradicted for this single transferred run and these fixed development panels. The expected selective improvement in negative-angle outcomes while preserving aggregate success was not observed: both measured checkpoints were below the parent on both panels, and the selected task-reference failures extended beyond the oversampled sector. This weakens the explanation that insufficient sector exposure was the primary residual cause. It does not establish that directional control or representation is causal, because the intervention was tested in one run and the remaining checkpoints were unmeasured. | working working; code revert |
| 4 | Replicate the current method from fresh initialization / parent fresh | Replicate the current method from fresh initialization | 2 measured checkpoints; 22 unmeasured checkpoints; research_evaluation/research_evaluation: 2 measurements; task_reference/task-reference-v1: 1 measurement | Contradicted for this fresh run and the measured development panels, with the conclusion scoped to this replication. The expected approximately 98% task-reference and near-99% matched researcher results were not observed: the measured checkpoint was 96.5% and both researcher measurements were 96%, while the parent was 99% on the same researcher panel. The broader shift in task-reference failure geometry and the parent's paired advantage support the alternative that the strong experiment-2 outcome was not a reproducible property of this fresh run. This does not establish whether run-to-run variation, inherited weights, or another learning dynamic caused the difference, and unmeasured checkpoints cannot be treated as failures. | working working; code keep |
